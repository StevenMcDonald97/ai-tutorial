{
  "sectionId": "s03",
  "title": "TypeScript as Architecture: Contracts, Invariants & Type-Driven Design",
  "description": "Elevates TypeScript usage from typed JavaScript to a tool for encoding and enforcing architectural decisions at compile time — covering discriminated unions, generics, the any vs. unknown distinction, branded types, and the integration of compile-time type contracts with the runtime Suspense boundary system from Section 2.",
  "learningObjectives": [
    {
      "objectiveId": "s03-obj-01",
      "description": "Define discriminated union types that model all valid states of a UI feature.",
      "linkedCompetencies": ["typescript-react-contracts"]
    },
    {
      "objectiveId": "s03-obj-02",
      "description": "Write generic React components and custom hooks that preserve full type inference at the call site.",
      "linkedCompetencies": ["typescript-react-contracts"]
    },
    {
      "objectiveId": "s03-obj-03",
      "description": "Evaluate a given set of prop types and identify where any, missing narrowing, or loose types create architectural contract holes.",
      "linkedCompetencies": ["typescript-react-contracts"]
    },
    {
      "objectiveId": "s03-obj-04",
      "description": "Apply branded/nominal types to distinguish domain entities that share the same primitive representation.",
      "linkedCompetencies": ["typescript-react-contracts"]
    },
    {
      "objectiveId": "s03-obj-05",
      "description": "Explain how discriminated union state types integrate with Suspense and Error Boundary patterns from Section 2 to eliminate impossible UI states.",
      "linkedCompetencies": ["typescript-react-contracts", "async-ui-architecture"]
    }
  ],
  "motivationHooks": [
    {
      "type": "real-world-stakes",
      "prompt": "Most React developers use TypeScript to annotate props. That's the smallest return on the investment. TypeScript's real architectural value is encoding constraints — making impossible states unrepresentable, making design intent visible to future maintainers, and catching category errors at compile time rather than in production. When you treat the type layer as architecture, your types become documentation that can't go stale, and your compiler becomes a reviewer that never sleeps."
    },
    {
      "type": "prediction-gap",
      "prompt": "Before reading: a hook returns { isLoading: boolean; isError: boolean; data?: User }. A consumer writes: if (!isLoading && !isError) { display(data!) }. Is this safe? What runtime bug could still occur here despite the guards? Hold your answer — the answer reveals why the boolean flag pattern is an architectural problem, not just a style preference."
    }
  ],
  "furtherReading": [
    {
      "resourceId": "s03-fr-01",
      "title": "TypeScript Handbook: Narrowing",
      "resourceType": "website",
      "url": "https://www.typescriptlang.org/docs/handbook/2/narrowing.html",
      "author": "TypeScript Team",
      "rationaleNote": "The definitive reference for TypeScript's narrowing mechanisms — discriminants, type guards, instanceof, and the never type. Read the 'Discriminated Unions' and 'Exhaustiveness checking' sections specifically.",
      "linkedCompetencies": ["typescript-react-contracts"]
    },
    {
      "resourceId": "s03-fr-02",
      "title": "TypeScript Handbook: Generics",
      "resourceType": "website",
      "url": "https://www.typescriptlang.org/docs/handbook/2/generics.html",
      "author": "TypeScript Team",
      "rationaleNote": "Covers generic functions, interfaces, and constraints. The 'Generic Constraints' section is directly relevant to writing hook signatures that preserve type inference at call sites.",
      "linkedCompetencies": ["typescript-react-contracts"]
    },
    {
      "resourceId": "s03-fr-03",
      "title": "Effective TypeScript — Item 35: Prefer More Precise Alternatives to any",
      "resourceType": "book",
      "citation": "Vanderkam, D. (2021). Effective TypeScript: 83 Specific Ways to Improve Your TypeScript (2nd ed.). O'Reilly Media. Item 35.",
      "author": "Dan Vanderkam",
      "rationaleNote": "A precise treatment of when any is genuinely necessary vs. when unknown, type guards, or generics are the better architectural choice. One of the most practically useful items in the book for React architects.",
      "linkedCompetencies": ["typescript-react-contracts"]
    },
    {
      "resourceId": "s03-fr-04",
      "title": "Branded Types in TypeScript",
      "resourceType": "article",
      "url": "https://egghead.io/blog/using-branded-types-in-typescript",
      "author": "Matt Pocock",
      "rationaleNote": "A concise, practical guide to the branded type pattern with real-world examples. Covers the phantom brand technique, constructor functions at trust boundaries, and common applications beyond IDs.",
      "linkedCompetencies": ["typescript-react-contracts"]
    }
  ],
  "learningExperiences": [
    {
      "experienceId": "s03-exp-01",
      "title": "TypeScript as Architecture: Discriminated Unions, Generics & Contract Design",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 4,
        "ambiguity": 3
      },
      "estimatedDurationMinutes": 40,
      "linkedCompetencies": ["typescript-react-contracts", "async-ui-architecture"],
      "desiredCognitiveActions": ["prediction", "analysis", "comparison", "retrieval"],
      "contentBlocks": [
        {
          "blockId": "s03-cb-01",
          "type": "text",
          "content": "WHY THIS MATTERS\n\nMost React developers use TypeScript to annotate props. That's the smallest return on the investment. TypeScript's real architectural value is encoding constraints — making impossible states unrepresentable, making design intent visible to future maintainers, and catching category errors at compile time rather than in production. When you treat the type layer as architecture, your types become documentation that can't go stale, and your compiler becomes a reviewer that never sleeps."
        },
        {
          "blockId": "s03-cb-02",
          "type": "text",
          "content": "LEARNING OBJECTIVES\n\nBy the end of this section, you will be able to:\n1. Define discriminated union types that model all valid states of a UI feature.\n2. Write generic React components and custom hooks that preserve full type inference at the call site.\n3. Evaluate prop types and identify where any, missing narrowing, or loose types create architectural contract holes.\n4. Apply branded/nominal types to distinguish domain entities sharing the same primitive representation.\n5. Explain how discriminated union state types integrate with Suspense and Error Boundary patterns to eliminate impossible UI states."
        },
        {
          "blockId": "s03-cb-03",
          "type": "text",
          "content": "KEY TERMS & DEFINITIONS\n\nDiscriminated union — A union type where each member has a shared literal field (the 'discriminant') that TypeScript uses to narrow to the correct member. Example: type State = { status: 'loading' } | { status: 'success'; data: User } | { status: 'error'; error: Error }.\n\nBranded type — A technique for creating nominal typing in TypeScript's structural type system. A UserId and an OrderId that are both string at runtime become incompatible at the type level by attaching a phantom brand: type UserId = string & { __brand: 'UserId' }.\n\nType narrowing — The process by which TypeScript refines a broad type to a specific member within a conditional block. if (state.status === 'success') { /* TypeScript knows state.data exists here */ }.\n\nGeneric constraint — A bound on a type parameter that limits what types it can be. <T extends { id: string }> requires that T has an id field.\n\nunknown vs any — unknown is the type-safe alternative to any. A value typed as unknown cannot be used without first narrowing it; any bypasses the type system entirely for all downstream consumers.\n\nExhaustiveness check — A pattern using a never-typed default branch to ensure a switch or if-chain handles every member of a union. If a new member is added, the compiler flags the uncovered branch."
        },
        {
          "blockId": "s03-cb-04",
          "type": "text",
          "content": "PART 1: DISCRIMINATED UNIONS — ELIMINATING IMPOSSIBLE STATES\n\nThe most common TypeScript mistake in React is this:\n\n  // Three independent booleans — can contradict each other\n  interface DataState {\n    isLoading: boolean;\n    isError: boolean;\n    data?: User;\n    error?: Error;\n  }\n\nThis type allows { isLoading: true, isError: true, data: user } — a state that should be impossible. With three booleans you have eight possible combinations; only three or four are valid. The impossible combinations become runtime bugs.\n\nThe fix is a discriminated union:\n\n  // Every combination is valid by construction\n  type UserState =\n    | { status: 'idle' }\n    | { status: 'loading' }\n    | { status: 'success'; data: User }\n    | { status: 'error'; error: Error };\n\nNow TypeScript enforces at compile time that you only access data when status === 'success':\n\n  function UserProfile({ state }: { state: UserState }) {\n    switch (state.status) {\n      case 'idle':    return <EmptyState />;\n      case 'loading': return <Skeleton />;\n      case 'success': return <ProfileCard user={state.data} />; // data is safe\n      case 'error':   return <ErrorUI error={state.error} />;   // error is safe\n    }\n  }\n\nEXHAUSTIVENESS CHECKING — add a never assertion to catch unhandled cases when the union grows:\n\n  default:\n    const _exhaustive: never = state;\n    throw new Error(`Unhandled state: ${JSON.stringify(_exhaustive)}`);\n\nIf you add a { status: 'refreshing' } member later and forget to handle it, the compiler flags the never assignment."
        },
        {
          "blockId": "s03-cb-05",
          "type": "text",
          "content": "PART 2: GENERICS — TYPE-SAFE REUSABLE ABSTRACTIONS\n\nGenerics are not an advanced library author feature. They are the primary tool for building reusable, type-safe components and hooks in application code.\n\nGENERIC COMPONENT EXAMPLE — a typed list:\n\n  // Loses type information at the call site\n  function List({ items, renderItem }: { items: any[]; renderItem: (item: any) => ReactNode }) { ... }\n\n  // Preserves full type inference\n  function List<T>({\n    items,\n    renderItem,\n    keyExtractor,\n  }: {\n    items: T[];\n    renderItem: (item: T) => ReactNode;\n    keyExtractor: (item: T) => string;\n  }) {\n    return <ul>{items.map(item => <li key={keyExtractor(item)}>{renderItem(item)}</li>)}</ul>;\n  }\n\n  // Call site: TypeScript infers T = User from items\n  <List\n    items={users}\n    keyExtractor={u => u.id}\n    renderItem={u => <UserCard user={u} />}  // u is User — fully typed\n  />\n\nGENERIC CUSTOM HOOK EXAMPLE:\n\n  function useAsync<T>(fn: () => Promise<T>): AsyncState<T> {\n    const [state, setState] = useState<AsyncState<T>>({ status: 'idle' });\n    const execute = useCallback(async () => {\n      setState({ status: 'loading' });\n      try {\n        const data = await fn();\n        setState({ status: 'success', data });\n      } catch (error) {\n        setState({ status: 'error', error: error as Error });\n      }\n    }, [fn]);\n    return { ...state, execute };\n  }\n\n  // T is inferred from the return type of the passed function\n  const { status, data } = useAsync(() => fetchUser(id));\n  // data is User | undefined — not any\n\nThe generic makes the hook useful across every data type while preserving the type contract at every call site."
        },
        {
          "blockId": "s03-cb-06",
          "type": "text",
          "content": "PART 3: any IS AN ARCHITECTURAL HOLE\n\n  // any disables type checking for all downstream consumers\n  function processResponse(data: any) {\n    return data.user.name; // TypeScript won't catch data.user being undefined\n  }\n\n  // unknown forces narrowing before use\n  function processResponse(data: unknown) {\n    if (isUserResponse(data)) {\n      return data.user.name; // safe — narrowed by type guard\n    }\n    throw new Error('Unexpected response shape');\n  }\n\n  function isUserResponse(data: unknown): data is UserResponse {\n    return (\n      typeof data === 'object' &&\n      data !== null &&\n      'user' in data &&\n      typeof (data as any).user?.name === 'string'\n    );\n  }\n\nThe practical alternative to any in API boundary code is to validate the shape at the entry point (Section 7 covers Zod for this) and narrow to a known type. Everything inside the trust boundary can then use fully typed data."
        },
        {
          "blockId": "s03-cb-07",
          "type": "text",
          "content": "PART 4: BRANDED TYPES — PREVENTING DOMAIN CONFUSION\n\nTypeScript's type system is structural: two types with the same shape are interchangeable. This becomes a problem for domain identifiers:\n\n  // TypeScript accepts this — both are string\n  function assignTask(taskId: string, userId: string) { ... }\n  assignTask(userId, taskId); // arguments swapped — compiles fine\n\nBranded types add a phantom marker that makes these incompatible:\n\n  type UserId = string & { readonly __brand: 'UserId' };\n  type TaskId = string & { readonly __brand: 'TaskId' };\n\n  // Constructor functions that perform the cast once, at the trust boundary\n  function UserId(id: string): UserId { return id as UserId; }\n  function TaskId(id: string): TaskId { return id as TaskId; }\n\n  function assignTask(taskId: TaskId, userId: UserId) { ... }\n\n  assignTask(UserId('u_123'), TaskId('t_456')); // compiler error — correct\n  assignTask(TaskId('t_456'), UserId('u_123')); // correct order\n\nUse branded types wherever your domain has multiple entity types sharing the same primitive representation — IDs are the most common case, but monetary amounts (USD, EUR) and validated strings (EmailAddress, SlugString) are other common applications."
        },
        {
          "blockId": "s03-cb-08",
          "type": "text",
          "content": "PART 5: INTEGRATION WITH SUSPENSE AND ERROR BOUNDARIES\n\nSection 2 established Suspense and Error Boundaries as structural components that handle the async lifecycle. Discriminated union types are the compile-time complement — they ensure that when you're inside the 'success' branch, the type system guarantees data is present.\n\nWith a Suspense-compatible library, the component only ever runs in the success state:\n\n  // The component is only rendered when data is present\n  // Suspense handles loading, Error Boundary handles errors\n  function UserProfile({ userId }: { userId: UserId }) {\n    const user = useUser(userId); // User — not User | undefined | Error\n    return <ProfileCard user={user} />;\n  }\n\nWithout Suspense, you handle all states manually, and a discriminated union enforces correct handling:\n\n  function UserProfileWithState({ userId }: { userId: UserId }) {\n    const state = useUserState(userId); // UserState discriminated union\n    if (state.status !== 'success') {\n      return <StateHandler state={state} />;\n    }\n    // TypeScript narrows state.data here — guaranteed present\n    return <ProfileCard user={state.data} />;\n  }\n\nBoth patterns are valid. The key insight: the type system and the boundary system solve the same problem at different layers — impossible compile-time states vs. impossible runtime states. Using them together eliminates an entire class of bugs."
        },
        {
          "blockId": "s03-cb-09",
          "type": "text",
          "content": "COMMON ERRORS\n\nError 1: Using as to suppress type errors\nconst user = data as User is the same architectural problem as any — it bypasses type checking without providing safety. Use as only at validated trust boundaries (e.g. after a Zod parse). Inside application logic, narrowing should come from type guards or discriminated union checks.\n\nError 2: Putting all types in a global types.ts file\nTypes should live close to the code that owns them. A global types file becomes a grab-bag with no clear ownership, creating merge conflicts and making it hard to reason about what depends on what. Co-locate types with their feature module.\n\nError 3: Optional chaining as a substitute for narrowing\nstate.data?.name compiles but doesn't verify that accessing data is valid in the current state. Use discriminated unions to narrow to the correct state first, then access properties directly without optional chaining."
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s03-int-01",
          "interactionType": "prediction",
          "prompt": "Before reading: a hook returns { isLoading: boolean; isError: boolean; data?: User }. A consumer writes:\n\n  if (!isLoading && !isError) { return <ProfileCard user={data!} /> }\n\nIs the non-null assertion (!) safe here? What runtime bug could still occur despite the guards?\n\nA) It is safe — if both flags are false, data is guaranteed to be present.\nB) It is unsafe — data can be undefined even when both flags are false, making the ! a real suppressed error.\nC) It is safe as long as the API never returns null for the data field.\nD) It is unsafe, but only in React 18+ because of concurrent rendering.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct. The type { isLoading: boolean; isError: boolean; data?: User } allows data to be undefined independently of the boolean flags. All three can be simultaneously false — for example, in the initial 'idle' state before any fetch has been triggered. The type system has no way to know that 'not loading and not error' implies data is present, because the relationship between the flags and data is not encoded in the type. The non-null assertion ! tells TypeScript to trust the developer's claim that data is defined — but the type provides no guarantee of that. A discriminated union with { status: 'success'; data: User } makes it structurally impossible to access data outside the success branch.",
            "correctiveFeedback": "The ! is suppressing a real potential null/undefined access. The type allows data to be undefined when both flags are false — it is not guaranteed. A discriminated union would make this impossible.",
            "hints": ["Can data be undefined even when isLoading is false and isError is false?", "Think about the initial state before any fetch has run."]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in your answer? (1 = guessing, 5 = certain)"
          }
        },
        {
          "interactionId": "s03-int-02",
          "interactionType": "multiple-choice",
          "prompt": "Which of the following is the correct way to prevent UserId and TeamId from being accidentally swapped in function arguments, given that both are string at runtime?\n\nA) Create a TypeScript interface UserId extends String {} for each.\nB) Use a branded type: type UserId = string & { readonly __brand: 'UserId' }.\nC) Use runtime validation to check the format of each string.\nD) Use enum UserId {} to create distinct types.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct. Branded types create nominal distinctions at the type level with zero runtime overhead. The __brand property is a phantom field — it exists only in the type system and is erased at compile time. This means TypeScript treats UserId and TeamId as incompatible even though both are plain strings at runtime. Option A fails because TypeScript's structural type system means any string would satisfy the interface extension. Option C adds runtime cost and doesn't prevent compile-time mistakes. Option D creates a distinct type but with a different value representation than string — branded types preserve the string representation while adding compile-time distinction.",
            "correctiveFeedback": "Branded types: type UserId = string & { readonly __brand: 'UserId' }. The brand is phantom — zero runtime cost, pure compile-time distinction.",
            "hints": ["The goal is a compile-time error with zero runtime overhead.", "TypeScript's structural system means two types with the same shape are interchangeable — unless you add a phantom property."]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in this answer?"
          }
        },
        {
          "interactionId": "s03-int-03",
          "interactionType": "scenario-analysis",
          "prompt": "A function is typed: function processApiResponse(data: any). A colleague says: 'any is fine here — it's just the API boundary, and we handle it carefully.' Explain the architectural problem with any at this location and describe the correct alternative approach.",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "The problem: any propagates silently. Every component, hook, and utility that receives data from processApiResponse now operates with any — TypeScript stops checking property access, function call signatures, and assignability for that entire data flow. A typo like data.user.naem compiles without error. This is not local to the boundary — it infects the entire downstream graph.\n\nThe correct approach: type the parameter as unknown, which forces the developer to validate and narrow before use. A type guard or Zod schema parse at the trust boundary produces a fully typed value. Everything downstream operates in full type safety. The validation work is local, explicit, and once — not silently deferred to every consumer.\n\nSection 7 will cover Zod as the production pattern for this: const result = schema.parse(data) returns a fully typed value and throws on invalid shape — a type-safe trust boundary in one line.",
            "correctiveFeedback": "any propagates downstream, silently disabling type checking for all consumers. Use unknown at the boundary, then narrow with a type guard or Zod parse to produce a fully typed value.",
            "hints": ["What does TypeScript do when you pass an any value to a function that expects a specific type?", "How is unknown different from any in terms of what you can do with the value before narrowing?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How well did you capture the downstream propagation problem?"
          }
        },
        {
          "interactionId": "s03-int-04",
          "interactionType": "scenario-analysis",
          "prompt": "REINFORCING SECTION 2 — connecting discriminated unions to Suspense boundaries: How do discriminated union state types and Suspense boundaries solve the same problem at different layers? What problem does each solve, and when would you use one without the other?",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "Both eliminate impossible UI states — but at different layers.\n\nDiscriminated unions: compile-time. They prevent you from writing code that accesses data during loading, or renders an error component when there is no error. The TypeScript compiler enforces correct handling at every call site. The cost: you must explicitly handle each state in your component logic.\n\nSuspense boundaries: runtime structural. They remove the loading and error states from components entirely — the component only runs when data is ready. The boundary handles the fallback and error rendering structurally in the component tree, not inline in the component logic.\n\nWith Suspense: the component only ever sees the 'success' case — the type can reflect this (no union needed, just the success shape). Without Suspense: the component handles all states inline, and a discriminated union makes that handling exhaustive and safe.\n\nYou'd use discriminated unions alone when not using Suspense-compatible libraries. You'd use Suspense alone when the data library handles async states structurally. The combination — discriminated union at the hook level, Suspense boundary at the tree level — eliminates impossible states at both layers.",
            "correctiveFeedback": "Discriminated unions: compile-time constraint on which state fields can be accessed in which branch. Suspense boundaries: runtime structure that removes loading/error states from component logic entirely. Both eliminate impossible states — one at type check time, one at render time.",
            "hints": ["When does each mechanism catch the problem — before shipping or at runtime?", "With Suspense, does the component inside the boundary ever see a loading state?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How clearly did you articulate the two-layer distinction?"
          }
        },
        {
          "interactionId": "s03-int-05",
          "interactionType": "multiple-choice",
          "prompt": "A component receives state: { isLoading: boolean; isError: boolean; data?: User }. The developer writes if (!state.isLoading && !state.isError) { return <ProfileCard user={state.data!} /> }. What is the architectural problem?\n\nA) The non-null assertion ! is unnecessary since data is always present when both flags are false.\nB) The type allows data to be undefined even when both flags are false — the ! suppresses a real type error.\nC) isLoading and isError should be checked with strict equality.\nD) There is no problem — the logic correctly guards against invalid states.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct — this was the opening prediction. Three boolean flags can all be false simultaneously while data is still undefined. Consider the initial state before any fetch has started: isLoading is false (no fetch in progress), isError is false (no error), and data is undefined (nothing has loaded yet). The type makes no guarantee that 'not loading and not error' implies data is present. The ! tells TypeScript to trust the developer's claim, but the type system cannot verify it. A discriminated union type State = { status: 'idle' } | { status: 'loading' } | { status: 'success'; data: User } | { status: 'error'; error: Error } makes this structurally impossible: data only exists on the success member, and TypeScript enforces access only within the success branch.",
            "correctiveFeedback": "B: data can be undefined when both flags are false (e.g. initial idle state before any fetch). The ! suppresses a real potential null access. A discriminated union makes this impossible.",
            "hints": ["What is the state of the component before the first API call has started?", "Can you construct a valid state object where both booleans are false but data is undefined?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "Has your confidence on this question changed since the opening prediction?"
          }
        }
      ],
      "workedExamples": [
        {
          "exampleId": "s03-we-01",
          "problem": "A code review reveals this hook in a SaaS billing module: function useBillingData(accountId: string) { const [data, setData] = useState<any>(null); const [loading, setLoading] = useState(false); const [error, setError] = useState(false); ... return { data, loading, error }; }. Identify all architectural contract holes and rewrite the hook with correct types.",
          "steps": [
            "Step 1 — Identify the contract holes. (a) data: any — all downstream consumers lose type safety; TypeScript cannot catch property access errors. (b) error: boolean — callers can't display error details or distinguish error types; the error information is destroyed. (c) accountId: string — any string is accepted; a TeamId or raw UUID from the wrong domain could be passed by mistake with no compile-time warning. (d) Three independent flags allow contradictory states: loading: true and error: true simultaneously is representable but meaningless.",
            "Step 2 — Define the discriminated union. type BillingState = | { status: 'idle' } | { status: 'loading' } | { status: 'success'; data: BillingRecord } | { status: 'error'; error: Error }; Every combination is now valid by construction. The data field only exists on 'success'; the error field only exists on 'error'.",
            "Step 3 — Apply the branded type to the parameter. type AccountId = string & { readonly __brand: 'AccountId' }; The function signature becomes: function useBillingData(accountId: AccountId): BillingState. Now passing a TeamId or a raw string produces a compiler error.",
            "Step 4 — Rewrite the implementation. Replace the three useState calls with a single useState<BillingState>({ status: 'idle' }). Replace the manual flag updates with setState({ status: 'loading' }), setState({ status: 'success', data }), and setState({ status: 'error', error }). The return type is now the discriminated union — no any leaks to consumers.",
            "Step 5 — Verify the improvements. (a) Impossible states are eliminated — loading and error cannot coexist. (b) Error details are preserved — callers can display error.message or distinguish error types. (c) AccountId branded type prevents wrong-domain ID bugs at compile time. (d) All downstream consumers get full type safety — accessing billingState.data outside the 'success' branch is a compiler error."
          ],
          "conclusion": "The rewrite is the same logic with four improvements: impossible states eliminated, error details preserved, branded AccountId prevents wrong-ID bugs, and no any propagates to consumers. The hook's type signature is now a contract — it communicates intent and enforces it."
        }
      ],
      "retrievalPractice": [
        {
          "retrievalItemId": "s03-retrieval-01",
          "retrievalType": "cued-recall",
          "prompt": "What makes a union type 'discriminated'? Give the structural requirement and show the minimum syntax for a discriminated union with three members.",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [3, 7, 14, 30],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s03-retrieval-02",
          "retrievalType": "application",
          "prompt": "Write an exhaustiveness check for a discriminated union with status values: 'pending', 'approved', 'rejected'. What TypeScript feature makes the check work, and what happens when a fourth status is added without updating the switch?",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [4, 10, 21],
            "decayParameters": {
              "decayRate": 0.32,
              "strengthMultiplier": 1.9,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s03-retrieval-03",
          "retrievalType": "contrast",
          "prompt": "Contrast any and unknown as parameter types for an API response handler. For each: what can the caller do with the value before narrowing, and what is the downstream impact on consumers of the function's return value?",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [5, 14, 30],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s03-retrieval-04",
          "retrievalType": "free-recall",
          "prompt": "From memory: what is the runtime cost of a branded type, and where in the codebase should the brand be applied (cast to the branded type)?",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [6, 14, 30],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.1,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s03-retrieval-05",
          "retrievalType": "application",
          "prompt": "Write the signature for a generic List component that accepts items of any type T, a keyExtractor function, and a renderItem function. At the call site with items={users}, what type does TypeScript infer for the item parameter in renderItem?",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [7, 21, 45],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s03-retrieval-06",
          "retrievalType": "scenario-analysis",
          "prompt": "INTERLEAVING — connecting TypeScript contracts to async boundaries (bridges ig-types-fp and reinforces S02): A component inside a Suspense boundary uses a Suspense-compatible hook: const user = useUser(userId). The hook is typed to return User (not User | undefined). How does the Suspense boundary make this type safe? What would the type need to be without a Suspense boundary, and how would the component logic change?",
          "difficulty": 4,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [8, 21, 45],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.2,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        }
      ],
      "transferExercises": [
        {
          "exerciseId": "s03-transfer-01",
          "context": "Ride-sharing dispatch system",
          "scenario": "A ride-sharing app has these entities: DriverId, RiderId, VehicleId, and TripId — all string at runtime. The current dispatch function signature is:\n\n  function dispatchRide(driverId: string, riderId: string, vehicleId: string): Promise<string>\n\nThe trip has these possible states: waiting for driver acceptance, driver en route to pickup, ride in progress, completed, cancelled. Some fields only exist in specific states: actualPickupTime (only after pickup), cancellationReason (only when cancelled), driverLocation (only during en-route and in-progress states).",
          "task": "1. Rewrite the dispatchRide signature using branded types for all ID parameters. What category of bug does this prevent, and what is the runtime cost?\n2. Model the trip state as a discriminated union. Show which fields are only valid in specific states. Use the discriminant field to make impossible field combinations unrepresentable.\n3. A UI component needs to show the driver's current location — only available during 'driver en route' and 'ride in progress' states. Show how the discriminated union makes the component's conditional logic type-safe. What would happen without the union?",
          "successCriteria": [
            "Branded types applied to all four ID types with correct phantom brand syntax; constructor functions at the trust boundary; zero-runtime-cost explanation",
            "Discriminated union with a shared status discriminant; actualPickupTime only on completed/in-progress states; cancellationReason only on cancelled; driverLocation only on enRoute and inProgress states",
            "Component uses a type narrowing check on status before accessing driverLocation — TypeScript allows the access only in the narrowed branches; without the union, the field would be marked optional and require a runtime check with no compile-time safety"
          ]
        }
      ],
      "masteryVerification": {
        "assessmentIds": ["assess-typescript-contracts"],
        "requiredPerformance": {
          "minimumAccuracy": 0.85,
          "minimumTransferScore": 0.80
        }
      }
    },
    {
      "experienceId": "s03-exp-02",
      "title": "Type Audit: Practical Activity",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 3,
        "conceptualComplexity": 3,
        "ambiguity": 3
      },
      "estimatedDurationMinutes": 20,
      "linkedCompetencies": ["typescript-react-contracts"],
      "desiredCognitiveActions": ["analysis", "retrieval", "reflection"],
      "contentBlocks": [
        {
          "blockId": "s03-cb-10",
          "type": "text",
          "content": "PRACTICAL ACTIVITY: TYPE AUDIT\n\nFind one data-fetching hook or API integration in a codebase you have access to (or use the billing hook from the worked example). Apply this checklist:\n\n1. Are loading/error/data represented as a discriminated union or as independent flags?\n2. Are any IDs typed as bare string or number that could benefit from branding?\n3. Is any or unconstrained unknown used anywhere in the return type?\n4. Does each component that consumes this hook only access fields that are guaranteed present in its current state?\n\nRewrite the hook (or the types around it) to close the holes you find."
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s03-int-06",
          "interactionType": "reflection",
          "prompt": "After completing the type audit: what did you find? For each hole you identified, classify it as one of: (a) impossible state — independent flags that can contradict, (b) any propagation — type safety loss to consumers, (c) domain confusion — unbranded IDs, or (d) optional chaining substitute — accessing fields without narrowing. Which category was most common in the code you audited?",
          "required": false,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "The most common findings in real codebases, roughly in order of frequency: (1) Independent flags — the boolean flag pattern is extremely common, especially in code written before discriminated unions were well-understood. (2) any propagation — often at API boundaries, sometimes from third-party SDK types that use any internally. (3) Optional chaining substitute — data?.user?.name everywhere instead of narrowing to the success state first. (4) Domain confusion — less common but highest impact when found; a wrong ID passed to an API call causes a confusing server error rather than a compile-time error.\n\nIf you found any or unconstrained unknown in return types: trace where it goes. Count how many components receive it downstream. That count is the blast radius of the architectural hole.",
            "correctiveFeedback": "Common findings: boolean flags (impossible states), any at API boundaries (downstream propagation), optional chaining instead of narrowing. Each has a specific fix: discriminated union, unknown + type guard, and narrowing guard respectively.",
            "hints": ["For each finding, ask: what runtime bug does this enable that the type system currently cannot catch?"]
          },
          "confidencePrompt": {
            "enabled": false,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How thoroughly did you audit the hook?"
          }
        }
      ],
      "workedExamples": [],
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-typescript-contracts"],
        "requiredPerformance": {
          "minimumAccuracy": 0.80,
          "minimumTransferScore": 0.75
        }
      }
    },
    {
      "experienceId": "s03-exp-03",
      "title": "Self-Guided: Enable strict Mode and Address Real Compiler Errors",
      "experienceType": "self-guided-exercise",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 3,
        "ambiguity": 4
      },
      "estimatedDurationMinutes": 20,
      "linkedCompetencies": ["typescript-react-contracts"],
      "desiredCognitiveActions": ["analysis", "reflection", "synthesis"],
      "contentBlocks": [],
      "interactionSequence": [],
      "workedExamples": [],
      "selfGuidedExercise": {
        "exerciseId": "s03-sge-01",
        "realWorldContext": "A TypeScript React codebase you have access to — either a client project, a personal project, or a public open-source repository. Alternatively, the TypeScript Playground at typescriptlang.org with a component you write from scratch.",
        "objective": "Apply TypeScript strict mode to surface hidden type holes in real code, classify each error by architectural category, and fix them correctly without using as casts or any suppressions.",
        "instructions": [
          "Pick one feature module in a TypeScript React codebase you have access to. If you don't have a codebase available, go to the TypeScript Playground (typescriptlang.org/play) and write a ~50-line component with deliberate type holes — boolean flags instead of a discriminated union, an any-typed API response, an unbranded ID parameter.",
          "If using a real codebase: open tsconfig.json and set 'strict': true if it is not already enabled. Run the TypeScript compiler (npx tsc --noEmit) and note the first 5 errors.",
          "For each of the 5 errors, classify it as one of: (a) impossible state — independent boolean flags that should be a discriminated union, (b) any propagation — any in a return type or parameter that needs unknown + narrowing, (c) domain confusion — unbranded IDs that should be nominal types, or (d) missing narrowing — accessing a field that may not exist in the current state.",
          "Fix each error correctly. Rules: no as casts inside application logic (only at validated trust boundaries), no any suppressions, no @ts-ignore comments. Use discriminated unions, type guards, branded types, or unknown + narrowing as appropriate.",
          "For each fix, write one sentence explaining what category of runtime bug this type improvement prevents."
        ],
        "estimatedRealWorldDurationMinutes": 90,
        "materialsOrPrerequisites": [
          "A TypeScript React codebase with at least one data-fetching hook or API integration, OR access to the TypeScript Playground",
          "TypeScript compiler installed (npx tsc) or the TypeScript Playground"
        ],
        "reflectionPrompts": [
          "How many of the 5 errors were real bugs hiding behind incorrect types, vs. loose types that needed tightening without causing a current runtime bug?",
          "Which fix required the most change to the consuming code — and what does that tell you about how far the original type hole had propagated?",
          "If you found any as casts already in the codebase, where were they? Were they at validated trust boundaries (acceptable) or inside application logic (architectural hole)?",
          "After fixing the errors, how has the type contract of the module changed? What can a future maintainer now rely on that they couldn't before?"
        ],
        "verification": {
          "mode": "reflection-submission",
          "isGating": false,
          "selfAssessmentCriteria": [
            "I found and classified at least 5 compiler errors (or introduced and fixed 5 deliberate type holes in the playground)",
            "I fixed each error without using as casts inside application logic, any suppressions, or @ts-ignore",
            "For each fix I wrote one sentence explaining the runtime bug category it prevents"
          ]
        },
        "linkedCompetencies": ["typescript-react-contracts"]
      },
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-typescript-contracts"],
        "requiredPerformance": {
          "minimumAccuracy": 0.80,
          "minimumTransferScore": 0.80
        }
      }
    }
  ]
}
