{
  "sectionId": "s05",
  "title": "State Topology: Ownership, Classification & Flow Design",
  "description": "Develops the core architectural skill of classifying state by kind and assigning it to the correct ownership layer — covering the six state categories, the colocation principle, server vs. client state lifecycle differences, Context as a dependency injection mechanism (not a state manager), and the correct resolution strategies for prop drilling.",
  "learningObjectives": [
    {
      "objectiveId": "s05-obj-01",
      "description": "Classify a given set of application state variables by kind: server cache, global client, local UI, derived, ephemeral, and form.",
      "linkedCompetencies": ["state-topology"]
    },
    {
      "objectiveId": "s05-obj-02",
      "description": "Design a state ownership map for a realistic SaaS feature.",
      "linkedCompetencies": ["state-topology", "architectural-judgment"]
    },
    {
      "objectiveId": "s05-obj-03",
      "description": "Explain the architectural consequences of placing server state in a client global store versus a dedicated server-cache layer.",
      "linkedCompetencies": ["state-topology", "library-ecosystem"]
    },
    {
      "objectiveId": "s05-obj-04",
      "description": "Explain why the Context API is a dependency injection mechanism rather than a state manager, and implement the value/dispatch separation pattern to prevent unnecessary consumer re-renders.",
      "linkedCompetencies": ["state-topology", "react-rendering-model"]
    },
    {
      "objectiveId": "s05-obj-05",
      "description": "Identify and resolve a prop-drilling anti-pattern using the most appropriate mechanism, justifying the choice.",
      "linkedCompetencies": ["state-topology", "component-design-patterns"]
    }
  ],
  "motivationHooks": [
    {
      "type": "real-world-stakes",
      "prompt": "The most expensive architectural mistakes in React applications are state mistakes. State placed in the wrong layer causes prop drilling, stale data, unnecessary re-renders, over-complex stores, and synchronization bugs. Getting state topology right — classifying what kind of state you have, then assigning it to the correct ownership layer — is the single decision that most determines how maintainable a SaaS front end will be six months after launch."
    },
    {
      "type": "prediction-gap",
      "prompt": "Before reading: a developer fetches user data from an API and stores it in a Redux slice. Three weeks later, users report seeing deleted users in the list until they hard-refresh. What is the root cause? And separately: a ThemeContext provides { theme, setTheme } as one object. A button that only calls setTheme re-renders every time theme changes. Why, and what is the fix? Hold both answers — they reveal two of the most common state architecture mistakes in production React apps."
    }
  ],
  "furtherReading": [
    {
      "resourceId": "s05-fr-01",
      "title": "TanStack Query: Practical React Query",
      "resourceType": "article",
      "url": "https://tkdodo.eu/blog/practical-react-query",
      "author": "Dominik Dorfmeister (TkDodo)",
      "rationaleNote": "The most comprehensive practical guide to TanStack Query by one of its maintainers. Covers the server state vs. client state distinction with real examples, and explains why treating server data as client state leads to the staleness bugs described in Part 3.",
      "linkedCompetencies": ["state-topology", "library-ecosystem"]
    },
    {
      "resourceId": "s05-fr-02",
      "title": "Application State Management with React",
      "resourceType": "article",
      "url": "https://kentcdodds.com/blog/application-state-management-with-react",
      "author": "Kent C. Dodds",
      "rationaleNote": "A clear argument for the colocation principle and for using composition before reaching for Context or a global store. The 'Colocation' and 'Lifting State' sections directly support the ownership model in Part 2.",
      "linkedCompetencies": ["state-topology"]
    },
    {
      "resourceId": "s05-fr-03",
      "title": "How to use React Context effectively",
      "resourceType": "article",
      "url": "https://kentcdodds.com/blog/how-to-use-react-context-effectively",
      "author": "Kent C. Dodds",
      "rationaleNote": "The value/dispatch separation pattern from Part 4 is derived from this article. Read it after the section to see the full implementation including custom hooks for each context and error boundaries for missing providers.",
      "linkedCompetencies": ["state-topology", "react-rendering-model"]
    },
    {
      "resourceId": "s05-fr-04",
      "title": "Before You Use Context",
      "resourceType": "website",
      "url": "https://react.dev/learn/passing-data-deeply-with-context#before-you-use-context",
      "author": "React Team",
      "rationaleNote": "React's official guidance on trying composition before reaching for Context. Directly supports the 'composition fix' in Part 5, with concrete examples of when each approach is appropriate.",
      "linkedCompetencies": ["state-topology", "component-design-patterns"]
    }
  ],
  "learningExperiences": [
    {
      "experienceId": "s05-exp-01",
      "title": "State Topology: Classification, Ownership & Context Design",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 5,
        "conceptualComplexity": 5,
        "ambiguity": 4
      },
      "estimatedDurationMinutes": 45,
      "linkedCompetencies": ["state-topology", "react-rendering-model", "library-ecosystem", "component-design-patterns"],
      "desiredCognitiveActions": ["prediction", "analysis", "comparison", "retrieval", "synthesis"],
      "contentBlocks": [
        {
          "blockId": "s05-cb-01",
          "type": "text",
          "content": "WHY THIS MATTERS\n\nThe most expensive architectural mistakes in React applications are state mistakes. State placed in the wrong layer causes prop drilling, stale data, unnecessary re-renders, over-complex stores, and synchronization bugs. Getting state topology right — classifying what kind of state you have, then assigning it to the correct ownership layer — is the single decision that most determines how maintainable a SaaS front end will be six months after launch."
        },
        {
          "blockId": "s05-cb-02",
          "type": "text",
          "content": "LEARNING OBJECTIVES\n\nBy the end of this section, you will be able to:\n1. Classify a given set of application state variables by kind: server cache, global client, local UI, derived, ephemeral, and form.\n2. Design a state ownership map for a realistic SaaS feature.\n3. Explain the architectural consequences of placing server state in a client global store.\n4. Explain why Context is a dependency injection mechanism, not a state manager, and implement the value/dispatch separation pattern.\n5. Identify and resolve a prop-drilling anti-pattern using the most appropriate mechanism."
        },
        {
          "blockId": "s05-cb-03",
          "type": "text",
          "content": "KEY TERMS & DEFINITIONS\n\nState topology — The map of what state exists in an application, what kind each piece is, and which layer owns it.\n\nServer cache state — Data that originates on the server and is cached client-side. Has unique lifecycle properties: staleness, background refetch, cache invalidation. Examples: user records, paginated lists, dashboard metrics.\n\nGlobal client state — Client-owned state that multiple unrelated parts of the UI need simultaneously. Examples: current user session, theme preference, notification queue.\n\nLocal UI state — State relevant only to one component or a small local subtree. Examples: whether a dropdown is open, a hover state, a local toggle.\n\nDerived state — State computed from other state. Should never be stored independently — recompute or memoize it. Examples: filtered lists, totals, formatted values.\n\nEphemeral state — Transient state with a very short lifetime, typically tied to a single interaction. Examples: tooltip visibility, drag position, animation state.\n\nForm state — The specific lifecycle of user input from first keystroke through validation to submission. Architecturally distinct from all other state kinds (covered in Section 7).\n\nProp drilling — Passing props through intermediate components that don't use them, solely to deliver data to a deeply nested consumer. A symptom of incorrect state placement.\n\nColocation — Placing state as close as possible to the components that use it. The first principle of state ownership."
        },
        {
          "blockId": "s05-cb-04",
          "type": "text",
          "content": "PART 1: THE STATE CLASSIFICATION FRAMEWORK\n\nBefore choosing a mechanism, classify the state. The classification determines the mechanism — not the other way around.\n\n  State Kind     | Owner                  | Mechanism\n  ---------------|------------------------|----------------------------------\n  Server cache   | Server + client cache  | TanStack Query / SWR\n  Global client  | Application            | Zustand / Redux Toolkit\n  Local UI       | Component              | useState / useReducer\n  Derived        | Nowhere (compute it)   | useMemo / selector\n  Ephemeral      | Component              | useState / useRef\n  Form           | Form library           | React Hook Form\n\nTHE CLASSIFICATION QUESTIONS:\n1. Does this data originate on the server? → Server cache. Don't put it in a global store.\n2. Do multiple unrelated components need it simultaneously? → Global client state.\n3. Does only one component (or a small local subtree) need it? → Local UI state. Keep it local.\n4. Can it be computed from other state? → Derived. Delete the stored copy.\n5. Does it live only for the duration of a single interaction? → Ephemeral.\n6. Is it user input in progress toward a submission? → Form state.\n\nTHE MOST COMMON MISCLASSIFICATION is treating server cache state as global client state — fetching data from an API and storing it in Redux. This leads to manual loading/error flag management, stale data bugs, and cache invalidation complexity that TanStack Query handles automatically."
        },
        {
          "blockId": "s05-cb-05",
          "type": "text",
          "content": "PART 2: STATE OWNERSHIP AND COLOCATION\n\nThe colocation principle: state should live as close as possible to the components that need it. Lift only as high as necessary.\n\n  // Lifted too high — app-level state for a local concern\n  function App() {\n    const [dropdownOpen, setDropdownOpen] = useState(false); // only UserMenu needs this\n    return <UserMenu dropdownOpen={dropdownOpen} setDropdownOpen={setDropdownOpen} />;\n  }\n\n  // Colocated — state lives where it's used\n  function UserMenu() {\n    const [dropdownOpen, setDropdownOpen] = useState(false);\n    return <>{/* ... */}</>;\n  }\n\nLifting state has a real cost: every state update re-renders the component that owns it and potentially its entire subtree. Unnecessary lifting creates unnecessary re-renders and couples unrelated components.\n\nTHE OWNERSHIP DESIGN PROCESS:\n1. List every piece of state the feature needs.\n2. Classify each using the framework above.\n3. For each piece, ask: 'What is the smallest subtree that contains all components needing this state?' That subtree's root owns it.\n4. Server cache state gets no owner in the component tree — TanStack Query owns it."
        },
        {
          "blockId": "s05-cb-06",
          "type": "text",
          "content": "PART 3: SERVER STATE VS. CLIENT STATE — WHY THEY'RE DIFFERENT\n\n  Property               | Client state | Server state\n  -----------------------|--------------|--------------------------------------------------\n  Source of truth        | The client   | The server\n  Goes stale?            | No           | Yes — other users may change it\n  Needs background refetch? | No        | Yes\n  Needs deduplication?   | No           | Yes — multiple components may request same data\n  Cache invalidation?    | Not applicable | Critical\n\nStoring server state in Redux or Zustand means you own all of the above manually. You write loading flags, error handling, refetch logic, cache expiry, and invalidation — all code that TanStack Query provides as first-class features.\n\n  // Server state stored in Redux — manual everything\n  const userSlice = createSlice({\n    name: 'user',\n    initialState: { data: null, loading: false, error: null },\n    reducers: { /* ... loading/success/error actions */ }\n  });\n  // Plus: thunks, background refetch logic, stale detection, cache invalidation...\n\n  // Server state in TanStack Query — lifecycle handled\n  function useUser(id: UserId) {\n    return useQuery({\n      queryKey: ['user', id],\n      queryFn: () => fetchUser(id),\n      staleTime: 5 * 60 * 1000, // 5 minutes\n    });\n  }\n\nTHE RULE: If it came from an API and will go back to the API, it's server cache state. Use a dedicated server cache layer."
        },
        {
          "blockId": "s05-cb-07",
          "type": "text",
          "content": "PART 4: CONTEXT — DEPENDENCY INJECTION, NOT STATE MANAGEMENT\n\nContext is the most architecturally misunderstood React API. The misconception: 'Context is a lightweight alternative to Redux.' The reality: Context is a dependency injection mechanism. Its job is to make a value available anywhere in a subtree without prop drilling — not to manage state changes efficiently.\n\nWHY CONTEXT IS NOT A STATE MANAGER:\nEvery consumer of a Context re-renders when the context value changes by reference. If you put a large object in Context and update any field, every consumer re-renders — even consumers that only use an unrelated field.\n\n  // Single context with everything — all consumers re-render on any change\n  const AppContext = createContext({ user, theme, notifications, dispatch });\n\nTHE VALUE/DISPATCH SEPARATION PATTERN:\nSplit Context into two: one for the stable state value, one for the dispatch function (which never changes reference):\n\n  const UserStateContext    = createContext<UserState | null>(null);\n  const UserDispatchContext = createContext<Dispatch<UserAction> | null>(null);\n\n  function UserProvider({ children }: { children: ReactNode }) {\n    const [state, dispatch] = useReducer(userReducer, initialUserState);\n    return (\n      <UserStateContext.Provider value={state}>\n        <UserDispatchContext.Provider value={dispatch}>\n          {children}\n        </UserDispatchContext.Provider>\n      </UserStateContext.Provider>\n    );\n  }\n\nComponents that only dispatch actions consume UserDispatchContext — they never re-render due to state changes.\n\nWHEN CONTEXT IS THE RIGHT TOOL:\n- Injecting theme, locale, or feature flags into a subtree.\n- Making a service (analytics client, router) available without prop drilling.\n- Sharing state within a tightly scoped feature subtree (e.g. compound component internal state).\n\nWHEN CONTEXT IS THE WRONG TOOL:\n- High-frequency state updates (every keystroke, animation frames).\n- State needed by many unrelated components across the app.\n- Complex state with many independent slices.\nUse a global store (Zustand, RTK) for those cases."
        },
        {
          "blockId": "s05-cb-08",
          "type": "text",
          "content": "PART 5: RESOLVING PROP DRILLING\n\nProp drilling is a symptom — diagnose before treating. The correct fix depends on why the drilling exists.\n\nDIAGNOSIS QUESTIONS:\n1. Is the state genuinely shared, or is it local state lifted too high? → Move it down (colocation).\n2. Is it shared across a well-defined feature subtree? → Context for that subtree.\n3. Is it truly cross-cutting (needed by unrelated parts of the app)? → Global store.\n4. Is the component hierarchy the actual problem? → Composition via children or render props.\n\nTHE COMPOSITION FIX — often overlooked:\n\n  // UserAvatar drilled through Layout and Header just to reach Nav\n  function App() {\n    const user = useCurrentUser();\n    return <Layout user={user} />;     // Layout doesn't use user\n  }\n  function Layout({ user }) {\n    return <Header user={user} />;     // Header doesn't use user\n  }\n  function Header({ user }) {\n    return <Nav user={user} />;        // Nav finally uses user\n  }\n\n  // Composition — App owns the assembly, no drilling\n  function App() {\n    const user = useCurrentUser();\n    return (\n      <Layout>\n        <Header>\n          <Nav user={user} />          {/* user passed directly to consumer */}\n        </Header>\n      </Layout>\n    );\n  }\n\nComposition via children eliminates drilling without introducing Context or a global store. It's the simplest fix and is underused."
        },
        {
          "blockId": "s05-cb-09",
          "type": "text",
          "content": "COMMON ERRORS\n\nError 1: Storing derived state\nconst [filteredUsers, setFilteredUsers] = useState([]) where filteredUsers is always computed from users and filter — this is a synchronization bug waiting to happen. Delete the stored copy. Use useMemo or compute inline.\n\nError 2: Putting server data in Redux/Zustand\nThis trades TanStack Query's automatic staleness management, background refetching, and deduplication for manual equivalents you write and maintain. The only exception: server data that genuinely needs to be transformed or combined with client state before storage.\n\nError 3: One large Context for everything\nA single AppContext holding user, theme, permissions, and feature flags means every consumer re-renders whenever any of those change. Split into focused, stable contexts. A theme context changes rarely; a notification context may change frequently — they should not be coupled.\n\nError 4: Reaching for Context before trying composition\nBefore adding a Context, ask if passing via children or lifting the JSX assembly to a common ancestor eliminates the drilling without any new mechanism. It often does.\n\nError 5: Treating all global state the same\nAuth session (rarely changes, read by many), notification queue (changes frequently, read by few), and UI preferences (changes occasionally, read by layout) have very different update frequencies and consumer counts. Group state by update frequency, not by 'it's global.'"
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s05-int-01",
          "interactionType": "prediction",
          "prompt": "Before reading: a developer fetches user data from GET /api/users and stores the result in a Redux slice. Three weeks later, users report seeing deleted users in the list until they hard-refresh the browser. Which of the following is the root cause?\n\nA) Redux doesn't support async operations natively.\nB) The server state stored in Redux has no staleness or invalidation mechanism — the cache never expires.\nC) The API response is being mutated inside the reducer.\nD) Redux Toolkit's createSlice doesn't support array state.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct. Redux has no concept of data staleness. Once data is stored in a Redux slice, it stays there indefinitely unless an action explicitly updates it. Without a background refetch mechanism, a cache expiry timer, or invalidation logic triggered by mutations, the cached list never updates — users see whatever was fetched on their last full page load. This is not a Redux limitation per se; it is a misclassification of state kind. Server state has a staleness lifecycle that Redux does not model. TanStack Query models it as a first-class concept: staleTime controls when data is considered fresh, and invalidateQueries triggers refetch after mutations.",
            "correctiveFeedback": "B: Redux has no staleness model. Data stored in a slice stays until explicitly updated by an action — there is no automatic expiry or background refetch. This is a state kind misclassification.",
            "hints": ["What does Redux do when data on the server changes — does it know?", "What would you have to write manually in Redux to keep the list fresh?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in your answer? (1 = guessing, 5 = certain)"
          }
        },
        {
          "interactionId": "s05-int-02",
          "interactionType": "scenario-analysis",
          "prompt": "Classify each of the following state variables for a SaaS project management app:\n\n1. List of projects from GET /api/projects\n2. Whether the sidebar is expanded\n3. Total project count\n4. Draft text in a new project name field\n5. Current user authentication session\n\nFor each, name the state kind, the correct mechanism, and give a one-sentence reason.",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "1. Project list: Server cache. It originates on the server, goes stale when other users create or delete projects, and needs background refetch. TanStack Query with a queryKey of ['projects'].\n\n2. Sidebar expanded: Local UI state. Only the sidebar component (and possibly a layout component) needs this. It doesn't go stale, doesn't need to be shared globally. useState in the layout or sidebar component.\n\n3. Total project count: Derived state. It is computed from the project list length — useMemo(() => projects?.length ?? 0, [projects]). Never store it separately; it will drift from the project list.\n\n4. Draft text: Form state (or ephemeral local UI). It's user input in progress, not yet submitted. React Hook Form or controlled useState local to the form. Discarded on submit success.\n\n5. Auth session: Global client state. It's needed by the header, project cards, permission checks — unrelated components across the app. Zustand store or a stable Context with the value/dispatch separation pattern.",
            "correctiveFeedback": "Projects: server cache (TanStack Query). Sidebar: local UI (useState). Count: derived (useMemo — never store). Draft: form/ephemeral (local useState or RHF). Auth session: global client (Zustand or Context).",
            "hints": ["Ask the classification questions in order: does it come from a server? Can it be computed? Is it needed globally?", "For the project count: what would happen if the list updates but the stored count doesn't?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in all five classifications?"
          }
        },
        {
          "interactionId": "s05-int-03",
          "interactionType": "multiple-choice",
          "prompt": "A ThemeContext provides { theme, setTheme } as a single object. A button component that only calls setTheme re-renders every time theme changes. What is the correct fix?\n\nA) Use React.memo on the button component.\nB) Split into ThemeValueContext and ThemeDispatchContext — consumers subscribe to only what they need.\nC) Replace Context with a global Zustand store.\nD) Wrap the context value in useMemo.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct — the value/dispatch separation pattern. The dispatch function (setTheme or a useReducer dispatch) has a stable reference across renders — it never changes. By splitting the context into ThemeValueContext (for theme) and ThemeDispatchContext (for setTheme), consumers that only dispatch never subscribe to the value context at all, so they never re-render when theme changes.\n\nOption A (React.memo) doesn't help because React.memo bails out on prop changes — but the re-render here is caused by context subscription, not props. Option D (useMemo on the context value) does create a stable object reference, but only when the inputs haven't changed — when theme does change, the memo updates and all consumers still re-render.",
            "correctiveFeedback": "B: split into value context and dispatch context. The dispatch function reference is stable — consumers that only dispatch never re-render for state changes. React.memo doesn't help with context-driven re-renders.",
            "hints": ["What is the reference stability of a useReducer dispatch function across renders?", "How does React.memo interact with context subscriptions?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in this answer?"
          }
        },
        {
          "interactionId": "s05-int-04",
          "interactionType": "scenario-analysis",
          "prompt": "A component tree has prop drilling: user flows through App → Layout → Header → Nav, but only Nav uses it. A junior developer suggests adding a UserContext. Before accepting this solution, what question should you ask first — and what alternative might make the Context unnecessary?",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "The question to ask first: 'Do Layout and Header actually need user as a prop, or are they just passing it through?' If Layout and Header have no use for user themselves — they're just conduits — the drilling is a JSX assembly problem, not a cross-cutting concern.\n\nThe composition fix: have App fetch user and pass it directly to Nav, with Layout and Header accepting children instead of a user prop:\n\n  function App() {\n    const user = useCurrentUser();\n    return (\n      <Layout>\n        <Header>\n          <Nav user={user} />\n        </Header>\n      </Layout>\n    );\n  }\n\nNow user is passed directly from its owner to its consumer. Layout and Header have no awareness of user at all. No Context, no provider, no consumer hook. This is always the right first question before reaching for any dependency injection mechanism.",
            "correctiveFeedback": "First question: do the intermediate components actually use the prop? If not, the composition fix (children pattern) eliminates drilling without any new mechanism. Context is warranted only when the consumer is genuinely not reachable via component composition.",
            "hints": ["What would Layout and Header look like if they accepted children instead of a user prop?", "When is prop drilling actually a JSX assembly problem vs. a cross-cutting concern?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How completely did you identify the composition alternative?"
          }
        },
        {
          "interactionId": "s05-int-05",
          "interactionType": "reflection",
          "prompt": "REINFORCING SECTIONS 1 & 4 — connecting state topology to rendering mechanics and FP principles (bridges ig-state-components group): Context consumers re-render when the context value changes by reference. Using the reference equality model from Section 1 and the immutability principle from Section 4, explain: (a) why wrapping a context value object in useMemo only partially solves the re-render problem, and (b) why the value/dispatch separation pattern is a more structurally correct fix.",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "useMemo on the context value object: useMemo({ user, theme }, [user, theme]) creates a new reference only when its dependencies change. When theme changes, the memo correctly produces a new reference — all consumers subscribed to this context re-render. So useMemo does not prevent re-renders when the underlying values change; it only prevents re-renders when the values haven't changed (stopping unnecessary re-renders from the parent's own renders). This is a partial optimization, not a structural fix.\n\nValue/dispatch separation is structurally correct because it exploits a fundamental reference stability guarantee: the dispatch function returned by useReducer (or setState from useState) has the same reference for the lifetime of the component. This is not a performance optimization — it is a structural property of React's API. Splitting the context means consumers that only dispatch can subscribe to a context whose value never changes reference — guaranteeing they never re-render from state changes. This is the immutability principle from Section 4 applied at the API design level: stable references mean stable renders.",
            "correctiveFeedback": "useMemo is partial: it still triggers re-renders when values change. Value/dispatch separation is structural: dispatch reference is permanently stable (React's guarantee), so dispatch-only consumers provably never re-render from state changes.",
            "hints": ["When does useMemo create a new reference?", "What is the reference stability guarantee of useReducer's dispatch function?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How well did you connect the reference equality model to both the partial and structural solutions?"
          }
        }
      ],
      "workedExamples": [
        {
          "exampleId": "s05-we-01",
          "problem": "A SaaS project dashboard needs: a list of projects (fetched from API, paginated), an active project filter (user-selected), the current user session (used in header, project cards, and permission checks), a 'new project' modal (open/closed), and a project count badge. Design the complete state ownership map.",
          "steps": [
            "Step 1 — List every piece of state. Project list, active filter, current user session, modal open/closed, project count badge. Five pieces — each needs classification before mechanism selection.",
            "Step 2 — Classify each. Project list: server cache — it originates on the server, goes stale when other users create or delete projects. Active filter: local UI / URL param — it belongs to this dashboard feature only; persisting it in the URL lets users share filtered views. Current user session: global client — used by unrelated components (header, cards, permission checks) simultaneously. Modal open/closed: ephemeral local UI — only the Dashboard component and the modal need this; its lifetime is a single interaction. Project count badge: derived — computed from projects.length; never store it separately.",
            "Step 3 — Assign ownership and mechanism. TanStack Query → project list (queryKey: ['projects', activeFilter]). URL param + useState → active filter (local to dashboard, reflected in URL). Zustand store → current user session (global, stable reference). useState in ProjectDashboard → modal open/closed (colocated ephemeral). useMemo → project count (derived — recomputes when projects changes).",
            "Step 4 — Sketch the implementation structure. ProjectDashboard owns: the modal state (useState), the active filter (useState or URL param). It reads: projects via useProjects(activeFilter) — TanStack Query. It reads: currentUser via useCurrentUser() — global store hook. It computes: projectCount via useMemo. No prop drilling — each consumer reads what it needs from the correct layer.",
            "Step 5 — Verify the design. Ask: is any server data in a global store? No. Is any derived state stored? No. Does any component receive props it doesn't use? No. Is any local state lifted higher than its smallest required subtree? No."
          ],
          "conclusion": "Each state piece is owned at the correct layer. The classification drives the mechanism — not developer familiarity or habit. The result: no prop drilling, no stale data bugs, no stored derived state, no unnecessary re-renders from misplaced global state."
        }
      ],
      "retrievalPractice": [
        {
          "retrievalItemId": "s05-retrieval-01",
          "retrievalType": "free-recall",
          "prompt": "From memory: name the six state kinds and their correct ownership layers and mechanisms. For each, give one example of state that belongs to that category.",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [3, 7, 14, 30],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s05-retrieval-02",
          "retrievalType": "contrast",
          "prompt": "Contrast server state and client state across four lifecycle properties: staleness, background refetch, deduplication, and cache invalidation. For each property, explain what happens when you treat server state as client state (store it in Redux).",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [4, 10, 21],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s05-retrieval-03",
          "retrievalType": "application",
          "prompt": "Why is Context a dependency injection mechanism rather than a state manager? Give one concrete example where using Context as a state manager causes a measurable performance problem, and name the architectural fix.",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [5, 14, 30],
            "decayParameters": {
              "decayRate": 0.32,
              "strengthMultiplier": 1.9,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s05-retrieval-04",
          "retrievalType": "cued-recall",
          "prompt": "Why should derived state never be stored independently? Describe the specific synchronization bug that occurs when derived state is stored as useState, and name the correct mechanism.",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [6, 14, 30],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.1,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s05-retrieval-05",
          "retrievalType": "application",
          "prompt": "A component tree passes a user prop through three levels of components that don't use it. List the four diagnosis questions for prop drilling and state which one applies here. What is the correct fix, and when would you choose Context instead?",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [7, 21, 45],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s05-retrieval-06",
          "retrievalType": "scenario-analysis",
          "prompt": "INTERLEAVING — connecting state topology to component design (bridges ig-state-components group, reinforces S01 context re-render, previews S06): A compound component (e.g. Tabs + Tabs.Tab + Tabs.Panel from Section 6) uses an internal Context to share activeTab state between its sub-components. Apply the state classification framework: what kind of state is activeTab? Why is Context the correct mechanism here specifically — even though Context is not a state manager — and why does this not create the re-render problem described in Part 4?",
          "difficulty": 4,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [8, 21, 45],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.2,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        }
      ],
      "transferExercises": [
        {
          "exerciseId": "s05-transfer-01",
          "context": "Hospital patient management system",
          "scenario": "A patient detail screen in a hospital system shows:\n- Patient demographics (fetched from a records API, updated by other staff in real time)\n- Active medications list (fetched separately, can be updated by prescribing physicians)\n- Current bed assignment (fetched, changes frequently during busy periods)\n- A nurse's in-progress handover note (typed, not yet submitted)\n- Allergy count badge (computed from patient record)\n- A 'flag for review' modal",
          "task": "1. Classify each piece of state. Note which ones are most likely to go stale and why.\n2. Assign ownership and mechanism. What staleTime strategy would you choose for each server cache piece, given the clinical stakes?\n3. The demographics and medications are fetched separately. A doctor changes the medications — how does TanStack Query's invalidation model handle this, versus how Redux would require you to handle it?\n4. The handover note is in-progress. What happens to it if the nurse accidentally navigates away? Is that acceptable in a clinical context, and what would you need to change architecturally to prevent data loss?",
          "successCriteria": [
            "Demographics: server cache, high staleTime risk (other staff update it) — staleTime should be short (30-60s) or refetchOnWindowFocus: true given clinical stakes",
            "Medications: server cache, highest staleness risk (changes by physicians directly affect patient safety) — very short staleTime or real-time subscription",
            "Bed assignment: server cache, frequent changes — short staleTime (15-30s) or polling",
            "Handover note: form/ephemeral state — local useState or RHF until submitted",
            "Allergy count: derived — useMemo from demographics data, never stored separately",
            "Flag for review modal: ephemeral local UI — useState in the patient detail component",
            "TanStack Query invalidation: on medication update mutation, call invalidateQueries(['medications', patientId]) — TanStack Query automatically refetches active queries for this key. Redux equivalent requires dispatching a fetch action manually, managing loading state, and ensuring all components that read medications are correctly subscribed to the refresh",
            "Handover note navigation loss: unacceptable in clinical context — architectural fix: persist draft to localStorage or auto-save to a drafts API endpoint; warn on navigate away with an unsaved changes dialog (useBlocker in React Router)"
          ]
        }
      ],
      "masteryVerification": {
        "assessmentIds": ["assess-state-topology"],
        "requiredPerformance": {
          "minimumAccuracy": 0.87,
          "minimumTransferScore": 0.82
        }
      }
    },
    {
      "experienceId": "s05-exp-02",
      "title": "State Topology Mapping: Practical Activity",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 4,
        "ambiguity": 4
      },
      "estimatedDurationMinutes": 20,
      "linkedCompetencies": ["state-topology", "architectural-judgment"],
      "desiredCognitiveActions": ["analysis", "synthesis", "comparison"],
      "contentBlocks": [
        {
          "blockId": "s05-cb-10",
          "type": "text",
          "content": "PRACTICAL ACTIVITY: STATE TOPOLOGY MAPPING\n\nGiven this SaaS CRM feature description:\n\n'A contact detail page shows the contact's profile (fetched from API), a list of their recent activities (fetched separately, paginated), an inline note editor (draft text, not yet saved), a tags input (local multi-select, saved on blur), a sidebar with the assigned team member (fetched as part of contact profile), and a merge duplicate modal triggered by a button.'\n\nWork through the following:\n1. List every piece of state implied by this description.\n2. Classify each using the six-category framework.\n3. Assign each to an ownership layer and mechanism.\n4. Identify any derived state that should not be stored.\n5. Draw the ownership map as a table: state | kind | owner | mechanism."
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s05-int-06",
          "interactionType": "scenario-analysis",
          "prompt": "For the CRM contact page activity: the 'assigned team member' is described as 'fetched as part of contact profile.' A developer proposes caching it separately in a global store so other parts of the app can access it. Evaluate this proposal using the state classification framework. What is the correct decision, and what would the developer need to justify to make the global store approach architecturally defensible?",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "The assigned team member is server cache state — it originates from the API as part of the contact profile. The first question to ask: do multiple unrelated components across the app genuinely need this data simultaneously, or does only the contact detail page use it?\n\nIf only the contact detail page uses it: the correct decision is to keep it in TanStack Query's cache under the contact's queryKey. No global store needed. TanStack Query already handles caching, staleness, and deduplication — putting it in a global store duplicates that layer manually.\n\nTo make the global store defensible, the developer would need to show: (1) at least two genuinely unrelated components across the app consume this data simultaneously (not just within the contact page subtree), AND (2) the data needs client-side transformation or combination with other client state that makes a pure server cache layer insufficient. Without both conditions, the global store adds complexity with no architectural benefit.",
            "correctiveFeedback": "Server cache state belongs in TanStack Query by default. The global store is only defensible if genuinely unrelated app sections consume this data simultaneously AND client-side transformation is required. Neither condition is described.",
            "hints": ["Does the description mention any other page or component that needs the assigned team member?", "What would TanStack Query's cache give you that a global store does not?"]
          },
          "confidencePrompt": {
            "enabled": false,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in your classification?"
          }
        }
      ],
      "workedExamples": [],
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-state-topology"],
        "requiredPerformance": {
          "minimumAccuracy": 0.82,
          "minimumTransferScore": 0.78
        }
      }
    },
    {
      "experienceId": "s05-exp-03",
      "title": "Self-Guided: Global Store Audit in a Real Codebase",
      "experienceType": "self-guided-exercise",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 4,
        "ambiguity": 4
      },
      "estimatedDurationMinutes": 25,
      "linkedCompetencies": ["state-topology", "library-ecosystem", "architectural-judgment"],
      "desiredCognitiveActions": ["analysis", "reflection", "synthesis"],
      "contentBlocks": [],
      "interactionSequence": [],
      "workedExamples": [],
      "selfGuidedExercise": {
        "exerciseId": "s05-sge-01",
        "realWorldContext": "A React codebase you work with that uses a global state store (Redux, Zustand, or Context) with at least one data-fetching concern stored in it.",
        "objective": "Apply the six-category state classification framework to a real global store, identify misclassified state, estimate the manual lifecycle code that a dedicated server cache layer would eliminate, and produce a one-paragraph architecture review diagnosis per finding.",
        "instructions": [
          "Open a React codebase you work with. Find one global state store — a Redux store, a Zustand store, or a large Context provider.",
          "List every piece of state in the store. For each piece, apply the six classification questions in order.",
          "Identify any state classified as 'server cache' — data that originated from an API call. For each such piece, estimate: how many lines of code exist solely to manage its loading flag, error state, staleness check, and refetch logic? Write that number down.",
          "Find at least one piece of derived state that is stored rather than computed. What state does it derive from? What synchronization bug could occur if the source state updates but the derived copy doesn't?",
          "For one of your server-cache findings: sketch what the equivalent code would look like using TanStack Query's useQuery. How many lines does it replace?",
          "Write a one-paragraph diagnosis for each finding — as if presenting to the team in an architecture review. Each paragraph should include: what the current pattern is, what architectural problem it creates, and what the correct alternative is."
        ],
        "estimatedRealWorldDurationMinutes": 90,
        "materialsOrPrerequisites": [
          "A React codebase with a global state store (Redux, Zustand, or Context) that includes at least one API-fetched value",
          "Access to the codebase to read the store definition and count lines of lifecycle management code"
        ],
        "reflectionPrompts": [
          "How many pieces of state in the store turned out to be server cache state masquerading as global client state? What is the total lines of manual lifecycle code managing them?",
          "For the derived state you found: describe the exact synchronization bug that could occur. Has this bug happened already in the codebase?",
          "If you replaced one server cache slice with TanStack Query, which features would you get for free (list them) and which would you lose (if any)?",
          "After this audit, what is the one change that would deliver the most architectural improvement to the codebase?"
        ],
        "verification": {
          "mode": "reflection-submission",
          "isGating": false,
          "selfAssessmentCriteria": [
            "I applied the six classification questions to every piece of state in the store and recorded results",
            "I identified at least one server cache state misclassification and estimated the manual lifecycle code",
            "I found at least one piece of derived state stored rather than computed, and described the synchronization bug it enables",
            "I wrote at least one paragraph-length architecture review diagnosis"
          ]
        },
        "linkedCompetencies": ["state-topology", "library-ecosystem", "architectural-judgment"]
      },
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-state-topology"],
        "requiredPerformance": {
          "minimumAccuracy": 0.82,
          "minimumTransferScore": 0.82
        }
      }
    }
  ]
}
