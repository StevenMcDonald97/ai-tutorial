{
  "sectionId": "s09",
  "title": "Testing Architecture: Strategy, Tooling & Contract Verification",
  "description": "Frames testing as an architectural discipline — designing a test suite that encodes behavioral contracts, survives refactoring, and costs proportionally less to maintain than the value it provides. Covers the three-layer testing trophy, React Testing Library integration tests, Mock Service Worker, renderHook for hook contracts, and a four-question framework for evaluating existing test suites.",
  "learningObjectives": [
    {
      "objectiveId": "s09-obj-01",
      "description": "Design a three-layer testing strategy (unit, integration, E2E) appropriate for a given SaaS front end's risk profile.",
      "linkedCompetencies": ["testing-architecture", "architectural-judgment"]
    },
    {
      "objectiveId": "s09-obj-02",
      "description": "Write React Testing Library integration tests that verify component behavior from a user interaction perspective, avoiding implementation detail coupling.",
      "linkedCompetencies": ["testing-architecture", "component-design-patterns"]
    },
    {
      "objectiveId": "s09-obj-03",
      "description": "Configure Mock Service Worker to intercept API calls in tests and explain why it is architecturally superior to mocking fetch or Axios directly.",
      "linkedCompetencies": ["testing-architecture", "library-ecosystem"]
    },
    {
      "objectiveId": "s09-obj-04",
      "description": "Write Jest unit tests for pure reducer functions and renderHook tests for custom hooks, explaining the coverage/value trade-off relative to integration tests.",
      "linkedCompetencies": ["testing-architecture", "functional-programming-react", "component-design-patterns"]
    },
    {
      "objectiveId": "s09-obj-05",
      "description": "Evaluate an existing test suite and identify tests that are brittle, redundant, or low-value relative to the risk they cover.",
      "linkedCompetencies": ["testing-architecture", "architectural-judgment"]
    }
  ],
  "motivationHooks": [
    {
      "type": "real-world-stakes",
      "prompt": "A test suite is an architectural artifact. It encodes assumptions about what behavior matters, what can change safely, and where the system is most likely to break. A poorly designed test suite is worse than no tests: it breaks on correct refactors, misses real regressions, and erodes the team's trust in testing itself. The goal is not coverage — it is a suite that catches regressions, survives refactoring, and costs proportionally less to maintain than the value it provides."
    },
    {
      "type": "prediction-gap",
      "prompt": "Before reading: a test suite has 90% code coverage, mostly snapshot tests. A developer refactors a button from <button> to a custom <IconButton> — same behavior, different implementation. Fifteen snapshot tests fail. The developer updates the snapshots and the suite is green. Two weeks later, the button's onClick handler is accidentally removed. No test catches it. Explain both phenomena. Hold your answer — it reveals the single most important distinction in testing architecture."
    }
  ],
  "furtherReading": [
    {
      "resourceId": "s09-fr-01",
      "title": "Write Tests. Not Too Many. Mostly Integration.",
      "resourceType": "article",
      "url": "https://kentcdodds.com/blog/write-tests",
      "author": "Kent C. Dodds",
      "rationaleNote": "The foundational argument for the testing trophy shape. Explains why integration tests provide the highest value-to-cost ratio for UI code and why the unit test pyramid is often inverted incorrectly for front-end applications.",
      "linkedCompetencies": ["testing-architecture"]
    },
    {
      "resourceId": "s09-fr-02",
      "title": "React Testing Library Documentation",
      "resourceType": "website",
      "url": "https://testing-library.com/docs/react-testing-library/intro/",
      "author": "Testing Library Team",
      "rationaleNote": "The canonical RTL reference. The 'Guiding Principles' section articulates the behavior-over-implementation philosophy that the query priority order and the avoidance of getByTestId are built on.",
      "linkedCompetencies": ["testing-architecture", "component-design-patterns"]
    },
    {
      "resourceId": "s09-fr-03",
      "title": "Mock Service Worker Documentation",
      "resourceType": "website",
      "url": "https://mswjs.io/docs/",
      "author": "MSW Team",
      "rationaleNote": "The official MSW docs. The 'Getting Started' section covers the Node.js setup for Jest tests. The 'Comparison' section explains how MSW differs from other mocking approaches — directly relevant to the architectural argument in Part 3.",
      "linkedCompetencies": ["testing-architecture", "library-ecosystem"]
    },
    {
      "resourceId": "s09-fr-04",
      "title": "Common Mistakes with React Testing Library",
      "resourceType": "article",
      "url": "https://kentcdodds.com/blog/common-mistakes-with-react-testing-library",
      "author": "Kent C. Dodds",
      "rationaleNote": "A systematic list of RTL anti-patterns with correct alternatives — using getByTestId instead of getByRole, using fireEvent instead of userEvent, not using findBy for async content. The practical complement to the lecture content.",
      "linkedCompetencies": ["testing-architecture"]
    }
  ],
  "learningExperiences": [
    {
      "experienceId": "s09-exp-01",
      "title": "Testing Architecture: Strategy, RTL, MSW & Contract Verification",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 3,
        "ambiguity": 3
      },
      "estimatedDurationMinutes": 45,
      "linkedCompetencies": ["testing-architecture", "architectural-judgment", "component-design-patterns", "functional-programming-react", "library-ecosystem"],
      "desiredCognitiveActions": ["prediction", "analysis", "comparison", "retrieval", "synthesis"],
      "contentBlocks": [
        {
          "blockId": "s09-cb-01",
          "type": "text",
          "content": "WHY THIS MATTERS\n\nA test suite is an architectural artifact. It encodes assumptions about what behavior matters, what can change safely, and where the system is most likely to break. A poorly designed test suite is worse than no tests: it breaks on correct refactors, misses real regressions, and erodes the team's trust in testing itself. The goal is not coverage — it is a suite that catches regressions, survives refactoring, and costs proportionally less to maintain than the value it provides."
        },
        {
          "blockId": "s09-cb-02",
          "type": "text",
          "content": "LEARNING OBJECTIVES\n\nBy the end of this section, you will be able to:\n1. Design a three-layer testing strategy appropriate for a given SaaS front end's risk profile.\n2. Write RTL integration tests verifying component behavior from a user interaction perspective.\n3. Configure MSW and explain why it is architecturally superior to mocking fetch directly.\n4. Write Jest unit tests for pure reducers and renderHook tests for custom hooks.\n5. Evaluate an existing test suite and identify brittle, redundant, or low-value tests."
        },
        {
          "blockId": "s09-cb-03",
          "type": "text",
          "content": "KEY TERMS & DEFINITIONS\n\nTesting trophy — A testing shape (Kent C. Dodds) that prioritizes integration tests over unit tests for UI code, with a small E2E layer and a targeted unit test base.\n\nReact Testing Library (RTL) — A testing utility that renders components into a real DOM and provides queries mirroring how users find elements: by role, label, text, and placeholder — not by CSS class or component name.\n\nMock Service Worker (MSW) — A library that intercepts HTTP requests at the service worker or Node.js level. Tests run against the real fetch/Axios code — only the network is mocked.\n\nrenderHook — An RTL utility that mounts a hook in a minimal wrapper component, allowing hooks to be called and tested in isolation.\n\nuserEvent — RTL's companion library for simulating user interactions (typing, clicking, tabbing) more accurately than fireEvent. Dispatches realistic browser events in sequence.\n\nContract test — A test that verifies a module's public interface rather than its implementation. Survives internal refactoring; breaks only when the contract changes.\n\nSnapshot test — A test that serializes a component's rendered output for comparison. High false-positive rate; low regression-catching value for behavioral testing."
        },
        {
          "blockId": "s09-cb-04",
          "type": "text",
          "content": "PART 1: TESTING STRATEGY — THE THREE-LAYER MODEL\n\nNo testing strategy fits every project. The right distribution depends on the application's risk profile.\n\n  E2E (Playwright / Cypress)\n    → Full browser, real network or MSW\n    → Tests: critical user journeys (signup, checkout, core workflow)\n    → Count: small (10-30). Slow, expensive to maintain.\n\n  Integration (Jest + RTL + MSW)\n    → Real DOM, mocked network\n    → Tests: feature behavior from user perspective\n    → Count: majority of the suite. Highest value-to-cost ratio.\n\n  Unit (Jest)\n    → Isolated functions, no rendering\n    → Tests: pure reducers, utility functions, Zod schemas, hooks\n    → Count: targeted. Only where logic is complex enough to warrant isolation.\n\nCALIBRATING BY RISK PROFILE:\n  App characteristic              | Implication\n  --------------------------------|------------------------------------------\n  Complex data transformations    | More unit tests for transformation logic\n  Many user-facing workflows      | More integration tests per workflow\n  Revenue-critical flows          | E2E coverage mandatory\n  Rapidly changing UI             | Fewer snapshot tests; more behavioral tests\n  Shared component library        | Unit + integration tests per component contract\n\nTHE MISTAKE TO AVOID: Inverting the pyramid — many unit tests for implementation details, few integration tests for actual behavior. This produces a suite that's expensive to maintain and doesn't catch the bugs that matter: broken user workflows."
        },
        {
          "blockId": "s09-cb-05",
          "type": "text",
          "content": "PART 2: INTEGRATION TESTS WITH REACT TESTING LIBRARY\n\nRTL's design philosophy: test what the user sees and does, not what the component does internally.\n\n  describe('InvoiceList', () => {\n    it('displays invoices fetched from the API', async () => {\n      render(<InvoiceList />, { wrapper: AppProviders });\n      expect(screen.getByRole('status')).toHaveTextContent(/loading/i);\n      const items = await screen.findAllByRole('row', { name: /invoice/i });\n      expect(items).toHaveLength(3);\n    });\n\n    it('filters invoices when user selects a status filter', async () => {\n      const user = userEvent.setup();\n      render(<InvoiceList />, { wrapper: AppProviders });\n      await screen.findAllByRole('row');\n      await user.click(screen.getByRole('button', { name: /filter/i }));\n      await user.click(screen.getByRole('option', { name: /paid/i }));\n      const rows = await screen.findAllByRole('row', { name: /invoice/i });\n      rows.forEach(row => {\n        expect(within(row).getByText(/paid/i)).toBeInTheDocument();\n      });\n    });\n\n    it('shows an error state when the API fails', async () => {\n      server.use(http.get('/api/invoices', () => HttpResponse.error()));\n      render(<InvoiceList />, { wrapper: AppProviders });\n      await screen.findByRole('alert');\n      expect(screen.getByText(/something went wrong/i)).toBeInTheDocument();\n    });\n  });\n\nRTL QUERY PRIORITY — use in this order:\n1. getByRole — most accessible, mirrors screen reader behavior.\n2. getByLabelText — for form fields.\n3. getByText — for non-interactive content.\n4. getByTestId — last resort only; couples tests to implementation.\n\nfindBy vs. getBy:\n- getBy*: synchronous; throws immediately if not found. Use for elements already in the DOM.\n- findBy*: asynchronous; waits up to timeout. Use for elements appearing after async operations."
        },
        {
          "blockId": "s09-cb-06",
          "type": "text",
          "content": "PART 3: MOCK SERVICE WORKER — NETWORK INTERCEPTION\n\nMSW intercepts HTTP at the network level — your application code makes real fetch or Axios calls; MSW intercepts them before they leave the process.\n\nSETUP:\n  // mocks/handlers.ts\n  export const handlers = [\n    http.get('/api/invoices', () =>\n      HttpResponse.json([\n        { id: 'inv_1', amount: 1200, status: 'paid',    client: 'Acme Corp' },\n        { id: 'inv_2', amount:  800, status: 'pending', client: 'Globex' },\n        { id: 'inv_3', amount: 2400, status: 'overdue', client: 'Initech' },\n      ])\n    ),\n    http.post('/api/invoices', async ({ request }) => {\n      const body = await request.json();\n      return HttpResponse.json({ id: 'inv_new', ...body }, { status: 201 });\n    }),\n  ];\n\n  // mocks/server.ts\n  export const server = setupServer(...handlers);\n\n  // jest.setup.ts\n  beforeAll(()  => server.listen());\n  afterEach(()  => server.resetHandlers()); // prevent handler leakage\n  afterAll(()   => server.close());\n\nWHY MSW IS ARCHITECTURALLY SUPERIOR TO MOCKING fetch:\nMocking fetch couples the test to the HTTP client implementation. If you swap Axios for fetch, the mock breaks even though the behavior didn't change. MSW mocks the network contract — the URL and response shape — which is the actual boundary between your app and the server. Tests that use MSW survive HTTP client refactors and test the actual request/response cycle your users experience."
        },
        {
          "blockId": "s09-cb-07",
          "type": "text",
          "content": "PART 4: UNIT TESTS — REDUCERS AND renderHook\n\nUnit tests are the right tool for pure logic: reducers, utility functions, Zod schema validation, and hook contracts.\n\nTESTING A PURE REDUCER:\n  describe('invoiceReducer', () => {\n    it('updates a single invoice without mutating others', () => {\n      const updated = { id: 'inv_1', status: 'paid', amount: 1200 };\n      const nextState = invoiceReducer(initialState, invoiceUpdated(updated));\n\n      expect(nextState.items[0].status).toBe('paid');\n      expect(nextState.items[1]).toBe(initialState.items[1]); // same reference — structural sharing\n      expect(nextState.items).not.toBe(initialState.items);   // new array — immutable\n    });\n  });\n\nTESTING A CUSTOM HOOK WITH renderHook:\n  describe('useMultiStepForm', () => {\n    it('initialises at step 0', () => {\n      const { result } = renderHook(() => useMultiStepForm(4));\n      expect(result.current.currentStep).toBe(0);\n      expect(result.current.isFirstStep).toBe(true);\n    });\n\n    it('clamps next() at the final step', () => {\n      const { result } = renderHook(() => useMultiStepForm(2));\n      act(() => { result.current.next(); result.current.next(); result.current.next(); });\n      expect(result.current.currentStep).toBe(1); // not 2 or 3\n    });\n\n    it('resets to step 0', () => {\n      const { result } = renderHook(() => useMultiStepForm(4));\n      act(() => { result.current.next(); result.current.next(); });\n      act(() => result.current.reset());\n      expect(result.current.currentStep).toBe(0);\n    });\n  });\n\nTHE VALUE/COST CALIBRATION:\nUnit tests for pure functions: high-value, low-maintenance — no side effects, no rendering, no network to mock.\nUnit tests for components (all deps mocked): low-value, high-maintenance — test implementation details, break on refactors.\nPREFER integration tests for components; reserve unit tests for pure logic."
        },
        {
          "blockId": "s09-cb-08",
          "type": "text",
          "content": "PART 5: EVALUATING AN EXISTING TEST SUITE — FOUR QUESTIONS\n\n1. DOES IT TEST BEHAVIOR OR IMPLEMENTATION?\n  // Tests implementation — breaks on any refactor\n  expect(wrapper.state('isOpen')).toBe(true);\n  expect(component.find('.dropdown-menu')).toHaveLength(1);\n\n  // Tests behavior — survives refactoring\n  expect(screen.getByRole('listbox')).toBeVisible();\n\n2. DOES IT VERIFY BEHAVIOR THAT COULD ACTUALLY REGRESS?\nA test asserting <Button /> renders a <button> element is not catching a realistic regression. Tests should cover behavior that has broken before, or behavior that would break silently.\n\n3. WOULD IT CATCH THE BUG IT'S SUPPOSEDLY PREVENTING?\nMany snapshot tests catch zero regressions — developers update the snapshot without reading the diff and the test 'passes.' If the failure mode is 'developer clicks update snapshots,' the test provides no safety.\n\n4. WHAT IS ITS FALSE-POSITIVE RATE?\nA brittle test fails on changes that don't break behavior. Brittle tests train developers to ignore test failures as noise, masking real regressions.\n\nSIGNALS OF A LOW-VALUE TEST SUITE:\n- Snapshot tests as the majority of component tests.\n- Tests that mock every dependency, leaving nothing real being tested.\n- 90%+ coverage with zero confidence the app works.\n- Tests that fail whenever a component is refactored, even when behavior is preserved."
        },
        {
          "blockId": "s09-cb-09",
          "type": "text",
          "content": "COMMON ERRORS\n\nError 1: Mocking fetch or Axios directly\nThis couples tests to the HTTP client implementation. Use MSW to mock the network boundary instead.\n\nError 2: Snapshot tests as a substitute for behavioral tests\nSnapshots catch HTML structure changes, not behavioral regressions. A changed class name fails a snapshot; a broken click handler doesn't.\n\nError 3: Testing a component by querying internal state\ncomponent.instance().state or Enzyme-style internal access couples tests to implementation. If you can't find an element the way a user would, the accessibility may itself be the problem.\n\nError 4: Not resetting MSW handlers between tests\nHandler overrides added via server.use() bleed into subsequent tests unless server.resetHandlers() is called in afterEach.\n\nError 5: Using getBy for async content\ngetByText('Loading...') throws immediately. For content appearing after async operations, use findByText — it waits up to the configured timeout."
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s09-int-01",
          "interactionType": "prediction",
          "prompt": "A test suite has 90% code coverage, mostly snapshot tests. A developer refactors a button from <button> to a custom <IconButton> — same behavior, different implementation. Fifteen snapshots fail. The developer updates them and the suite is green. Two weeks later, the onClick handler is accidentally removed. No test catches it. Which of the following best explains both phenomena?\n\nA) Snapshot tests are unreliable — they should be replaced with E2E tests.\nB) Snapshot tests verify DOM structure, not behavior. Structural refactors break them; behavioral regressions don't. High coverage from snapshots provides false confidence.\nC) The developer should not have updated the snapshots without code review.\nD) 90% coverage is insufficient — 100% coverage would have caught the onClick removal.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct. Snapshot tests serialize the DOM tree and compare it to a stored version. When the button's DOM representation changes (from <button> to the <IconButton>'s rendered output), the structural diff fails — even though behavior is identical. When the onClick handler is removed, the DOM structure is unchanged — the snapshot still matches. Snapshot tests cannot catch behavioral regressions because they make no assertion about what happens when the user interacts with the element. This is the fundamental limitation: coverage measures what code is executed, not whether behavior is correct. A behavioral RTL test — await user.click(screen.getByRole('button', { name: /submit/i })); expect(handler).toHaveBeenCalled() — catches the onClick removal regardless of DOM structure changes.",
            "correctiveFeedback": "B: snapshots verify structure, not behavior. Structure changes break them; behavioral regressions don't. Coverage measures execution, not correctness.",
            "hints": ["What does a snapshot test assert? What does it NOT assert?", "After removing onClick, does the DOM structure change?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in your answer? (1 = guessing, 5 = certain)"
          }
        },
        {
          "interactionId": "s09-int-02",
          "interactionType": "multiple-choice",
          "prompt": "A test uses screen.getByTestId('submit-btn') to find a button. A developer refactors the button from <button> to a custom <IconButton> and removes the data-testid. The test breaks. What does this indicate?\n\nA) The test is correctly catching a regression — the button's behavior changed.\nB) The test is brittle — it's coupled to an implementation detail rather than user-observable behavior.\nC) getByTestId is unreliable and should never be used.\nD) Custom components should always include data-testid for testability.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct. The button's behavior (submitting the form) didn't change — only its DOM implementation changed. The test is coupled to the data-testid attribute, which is an implementation detail. A test using getByRole('button', { name: /submit/i }) queries the element by its user-visible role and accessible name — the same way a screen reader or keyboard user would find it. This query survives the refactor because the button still has role='button' and the same accessible name, regardless of whether it's rendered by <button> or <IconButton>. Importantly: if the refactor accidentally removed keyboard accessibility, getByRole would catch that — the element would no longer have the correct role. getByTestId provides no such accessibility coverage.",
            "correctiveFeedback": "B: the test is brittle — coupled to a data-testid implementation detail. getByRole('button', { name: /submit/i }) survives the refactor and additionally tests accessibility.",
            "hints": ["Did the button's behavior change when it was refactored?", "What would getByRole('button', { name: /submit/i }) query — the DOM structure or the user-visible interface?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in this answer?"
          }
        },
        {
          "interactionId": "s09-int-03",
          "interactionType": "multiple-choice",
          "prompt": "Why is server.resetHandlers() called in afterEach rather than afterAll in an MSW test setup?\n\nA) afterAll doesn't support MSW's handler reset API.\nB) Per-test handler overrides (via server.use(...)) must be cleared after each test to prevent them from affecting subsequent tests.\nC) resetHandlers also clears the default handlers, so it must be called after each test to re-register them.\nD) MSW requires handlers to be reset before each network request.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct. When a test uses server.use(http.get('/api/invoices', () => HttpResponse.error())) to override the default handler for an error scenario, that override persists until explicitly removed. If resetHandlers() is called only in afterAll (once at the end of the entire suite), every subsequent test in the file runs with the error handler active — causing cascade failures that are extremely difficult to diagnose because the tests appear to have no relationship to each other. resetHandlers() in afterEach restores the original handlers after each individual test, ensuring each test starts with a clean, predictable network state. Note: resetHandlers() does NOT clear the default handlers — it only removes overrides added by server.use().",
            "correctiveFeedback": "B: server.use() overrides persist until removed. afterEach ensures each test starts with clean, original handlers. Cascade failures from handler leakage are notoriously hard to debug.",
            "hints": ["What happens to server.use() overrides when a test ends?", "If test 1 adds an error handler and test 2 expects success — what does test 2 see if resetHandlers() wasn't called?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in this answer?"
          }
        },
        {
          "interactionId": "s09-int-04",
          "interactionType": "scenario-analysis",
          "prompt": "A component unit test mocks its entire data layer: jest.mock('../hooks/useInvoices'), jest.mock('../hooks/usePermissions'), jest.mock('../utils/formatCurrency'). The test passes. A colleague says: 'This test mocks everything — what real code is actually being tested here?' Evaluate this concern and explain when this pattern is appropriate vs. when integration tests would provide more value.",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "The colleague is correct. When all dependencies are mocked, the test is only verifying that the component renders specific JSX given specific mocked return values — it's essentially testing that the mocking framework works. The real application code (useInvoices fetching, permission checking, currency formatting) is entirely bypassed. If useInvoices changes its interface, if the API response shape changes, if formatCurrency has a bug — none of these would cause this test to fail.\n\nThis pattern is appropriate for: (a) testing complex conditional rendering logic in isolation when the data layer is separately tested, (b) testing a pure presentational component where you want to verify visual behavior without the overhead of an async data layer setup.\n\nIntegration tests provide more value when: the component's behavior depends on the interaction between multiple units (hook + component + error handling), when you want to verify the API integration is correct, or when a behavioral regression would only appear in the combined system. An RTL + MSW test renders the real component with its real hooks, makes real fetch calls intercepted by MSW, and verifies behavior from the user's perspective — all real code executes.",
            "correctiveFeedback": "When everything is mocked, only the test doubles are being exercised — the real application code is absent. Integration tests with MSW keep the real hooks, formatCurrency, and rendering all under test, with only the network mocked.",
            "hints": ["If useInvoices's interface changes, does this mocked test catch it?", "What is the smallest useful unit of behavior for a data-displaying component?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How clearly did you identify what's actually being tested and when each approach is appropriate?"
          }
        },
        {
          "interactionId": "s09-int-05",
          "interactionType": "reflection",
          "prompt": "REINFORCING SECTIONS 4, 6 & 8 — connecting testing to FP principles, component patterns, and library ecosystem (bridges ig-testing-performance group, reinforces prior sections): The reducer unit test asserts expect(nextState.items[1]).toBe(initialState.items[1]) — same reference. Which Section 4 FP principle does this test verify? And the renderHook tests make no assumption about whether useState or useReducer is used internally — which Section 6 hook contract principle does this demonstrate? Finally, using Section 8's insight: why is MSW a better test architecture choice than mocking useQuery directly when testing a TanStack Query component?",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "Section 4 — structural sharing: The assertion toBe(initialState.items[1]) verifies that the reducer used structural sharing — the unchanged item was not cloned into a new reference, only the changed path produced new references. This is exactly Immer's guarantee: unchanged branches share references with the original state, making reference equality checks in useSelector and React.memo correct. The test verifies the FP invariant that pure reducers must maintain.\n\nSection 6 — hook contract stability: The renderHook tests call result.current.next() and assert on result.current.currentStep — they interact only with the public interface. Swapping useState for useReducer internally changes nothing the tests can see. This is the contract test principle: the interface is stable, the implementation is replaceable. The tests encode the behavioral contract, not the implementation.\n\nSection 8 — MSW vs. mocking useQuery: Mocking useQuery couples the test to the TanStack Query version and API. If TanStack Query v6 renames useQuery or changes its signature, mocked tests break even though behavior didn't. MSW sits at the network boundary — the URL and response shape — which is stable regardless of which library makes the request. The test survives a TanStack Query upgrade, an Axios→fetch migration, or a switch to SWR.",
            "correctiveFeedback": "Structural sharing (S4): toBe() verifies unchanged items aren't cloned. Hook contract stability (S6): tests interact only with the public interface, making internals replaceable. MSW architectural choice (S8): network boundary is stable across library changes; useQuery mock is not.",
            "hints": ["What does toBe() check — value equality or reference equality?", "Which TanStack Query API would break if you mocked useQuery directly and then upgraded the library?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How completely did you connect all three prior section principles to the testing examples?"
          }
        }
      ],
      "workedExamples": [
        {
          "exampleId": "s09-we-01",
          "problem": "Two tests exist for an InvoiceCard component. Audit each against the four-question framework and rewrite them correctly.",
          "steps": [
            "Step 1 — Examine the original tests. Test 1: expect(container).toMatchSnapshot(). Test 2: fireEvent.click(getByTestId('edit-button')); expect(onEdit).toHaveBeenCalledTimes(1).",
            "Step 2 — Apply the four-question audit to Test 1 (snapshot). Q1: Does it test behavior or implementation? Implementation — it tests DOM structure. Q2: Does it verify realistic regression risk? No — the snapshot fails on any visual change, including correct ones. Q3: Would it catch the bug it's preventing? No — a broken amount display or removed status badge wouldn't change the snapshot if the DOM structure is the same. Q4: False-positive rate? High — developers auto-update snapshots without reading the diff.",
            "Step 3 — Apply the four-question audit to Test 2 (getByTestId + fireEvent). Q1: Tests behavior partially, but uses implementation detail (getByTestId). Q2: Partially — it verifies the handler fires, but not what it's called with. Q3: Partially — it would catch a removed onClick, but not a wrong argument. Q4: Brittle — breaks when data-testid is removed or renamed.",
            "Step 4 — Rewrite both as a single behavioral test. Use userEvent instead of fireEvent (realistic events). Use getByRole instead of getByTestId (user-observable query). Assert on visible content (amount, status) not just render. Assert on handler call argument (invoice.id), not just call count.",
            "Step 5 — Verify the rewrite. Does it survive renaming the edit button's DOM element? Yes — getByRole queries accessible role. Does it catch a removed onClick? Yes — the click fires but the handler isn't called. Does it catch a wrong argument? Yes — toHaveBeenCalledWith('inv_1') verifies the exact argument. Does it catch a broken amount display? Yes — expects getByText('$1,200.00') to be in the document."
          ],
          "conclusion": "The rewrite consolidates two brittle, low-value tests into one behavioral test that: (a) survives DOM refactors, (b) catches behavioral regressions the snapshots couldn't, (c) provides accessibility coverage via getByRole, and (d) requires zero maintenance when the visual implementation changes. This is the core value proposition of the behavior-over-implementation testing philosophy."
        }
      ],
      "retrievalPractice": [
        {
          "retrievalItemId": "s09-retrieval-01",
          "retrievalType": "free-recall",
          "prompt": "From memory: describe the testing trophy shape. For each of the three layers, name the tooling, the target count, and the value-to-cost ratio. Why does it prioritise integration tests over unit tests for UI code?",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [3, 7, 14, 30],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s09-retrieval-02",
          "retrievalType": "cued-recall",
          "prompt": "State the RTL query priority order (four levels). For each, give one concrete example of when to use it and what it queries under the hood.",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [4, 10, 21],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s09-retrieval-03",
          "retrievalType": "contrast",
          "prompt": "Contrast mocking fetch directly vs. using MSW. What does each approach mock, what does each leave real, and which survives an HTTP client migration from Axios to fetch?",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [5, 14, 30],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s09-retrieval-04",
          "retrievalType": "application",
          "prompt": "Name the four questions for auditing a test suite. Apply all four to this test: expect(container).toMatchSnapshot(). What is the verdict, and what should replace it?",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [6, 14, 30],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.1,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s09-retrieval-05",
          "retrievalType": "application",
          "prompt": "When are unit tests preferable to integration tests for React code? Give two specific cases where unit tests provide higher value, and explain what makes them high-value rather than low-value in those cases.",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [7, 21, 45],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s09-retrieval-06",
          "retrievalType": "scenario-analysis",
          "prompt": "INTERLEAVING — connecting testing to performance architecture (bridges ig-testing-performance group, previews S10): A component uses React.lazy() for code splitting. When testing it: (a) does the test need to trigger the dynamic import, and how? (b) should the test verify the Suspense fallback appears while the chunk loads? (c) is this an integration test or an E2E test candidate? Use the testing trophy to justify the layer choice and explain the MSW/chunk loading interaction.",
          "difficulty": 4,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [8, 21, 45],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.2,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        }
      ],
      "transferExercises": [
        {
          "exerciseId": "s09-transfer-01",
          "context": "E-learning platform",
          "scenario": "An e-learning platform has three features to test:\n1. A video player that tracks watch progress and marks a lesson complete when 90% watched.\n2. A quiz component that submits answers to POST /api/quiz-attempts, shows a score, and blocks retakes for 24 hours.\n3. A course enrollment flow spanning three steps and two API calls — payment processing and enrollment confirmation.",
          "task": "1. For each feature, decide: unit test, integration test, E2E test, or a combination? Justify each based on risk profile and the type of behavior being verified.\n2. For the quiz submission: write the MSW handler structures (not full implementation) for (a) a successful submission showing the score, and (b) a 'retake blocked' response showing the correct error UI. Which RTL queries would you use to verify each outcome?\n3. The 'mark lesson complete' logic is isComplete(watchedSeconds, totalSeconds) => watchedSeconds / totalSeconds >= 0.9. Should this be a unit test or integration test? What would each look like?\n4. The enrollment flow is revenue-critical. What makes it an E2E candidate rather than an integration test? What would you accept as a minimum E2E coverage bar?",
          "successCriteria": [
            "Video player: integration test for the progress tracking UI behavior; unit test for the isComplete pure function. E2E not needed — no cross-system boundary beyond the already-mocked API.",
            "Quiz: integration tests cover the happy path and retake-blocked scenarios using MSW. MSW handlers: http.post('/api/quiz-attempts', () => HttpResponse.json({ score: 85 })) for success; http.post('/api/quiz-attempts', () => HttpResponse.json({ error: 'retake_blocked' }, { status: 403 })) for blocked. RTL queries: findByText(/85/) for score; findByRole('alert') for the blocked error.",
            "isComplete: unit test. It's a pure function — input → output, no rendering, no network. The unit test: expect(isComplete(90, 100)).toBe(true); expect(isComplete(89, 100)).toBe(false). An integration test would render the video player and advance time — higher maintenance, same coverage for this specific logic.",
            "Enrollment E2E: revenue-critical because it spans multiple systems (payment processor, enrollment service, email), involves real financial transactions, and a failure is immediately user-impacting. E2E minimum bar: at least one complete successful enrollment and one failed payment path. Integration tests cover individual step behavior; E2E covers the end-to-end state machine."
          ]
        }
      ],
      "masteryVerification": {
        "assessmentIds": ["assess-testing-architecture"],
        "requiredPerformance": {
          "minimumAccuracy": 0.85,
          "minimumTransferScore": 0.80
        }
      }
    },
    {
      "experienceId": "s09-exp-02",
      "title": "Write Three Tests for a Feature: Practical Activity",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 3,
        "conceptualComplexity": 3,
        "ambiguity": 3
      },
      "estimatedDurationMinutes": 20,
      "linkedCompetencies": ["testing-architecture", "component-design-patterns"],
      "desiredCognitiveActions": ["analysis", "synthesis", "reflection"],
      "contentBlocks": [
        {
          "blockId": "s09-cb-10",
          "type": "text",
          "content": "PRACTICAL ACTIVITY: WRITE THREE TESTS FOR A FEATURE\n\nGiven a <ProjectList> component that:\n- Fetches projects from GET /api/projects\n- Displays a loading skeleton while fetching\n- Renders a list of project cards when data arrives\n- Shows an error message with a retry button when the request fails\n- Allows filtering by project status via a dropdown\n\nWrite the following tests using RTL + MSW:\n1. An integration test verifying the happy path: data loads and renders correctly.\n2. An integration test verifying the error path: API failure shows error UI with a retry button that re-fetches on click.\n3. An integration test verifying filter behavior: selecting a status filter updates the displayed list.\n\nFor each test: use userEvent for interactions, findBy for async assertions, and getByRole as the primary query strategy."
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s09-int-06",
          "interactionType": "reflection",
          "prompt": "After writing the three tests: for the error path test, you clicked a retry button that triggered a new API request. Did your test need to reset the MSW handler to return success after the error? What does this tell you about how handler overrides interact with retries — and what is the correct MSW pattern to support this test scenario?",
          "required": false,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "Yes — the error path test typically needs to: (1) override the handler to return an error for the initial request, (2) assert the error UI appears, (3) restore the success handler before the retry click, (4) click retry, (5) assert the success UI appears.\n\nThe pattern to support this:\n\n  it('shows error then retries successfully', async () => {\n    const user = userEvent.setup();\n\n    // Override to error for first request\n    server.use(http.get('/api/projects', () => HttpResponse.error()));\n\n    render(<ProjectList />, { wrapper: AppProviders });\n    await screen.findByRole('alert');\n\n    // Restore success handler before retry\n    server.resetHandlers(); // removes the error override, restores default success handler\n\n    await user.click(screen.getByRole('button', { name: /retry/i }));\n    await screen.findAllByRole('listitem'); // success UI appears\n  });\n\nThe key insight: server.resetHandlers() within a test (not just in afterEach) is valid and useful when you need to change server behavior mid-test to simulate a recovery scenario. This is a legitimate pattern, not a test smell.",
            "correctiveFeedback": "Yes — call server.resetHandlers() mid-test after the error assertion to restore the success handler before clicking retry. This is a legitimate within-test handler management pattern for recovery scenarios.",
            "hints": ["After the error UI appears, what should the server return when the user clicks retry?", "Can server.resetHandlers() be called mid-test, not just in afterEach?"]
          },
          "confidencePrompt": {
            "enabled": false,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How completely did you handle the handler state for the retry scenario?"
          }
        }
      ],
      "workedExamples": [],
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-testing-architecture"],
        "requiredPerformance": {
          "minimumAccuracy": 0.80,
          "minimumTransferScore": 0.75
        }
      }
    },
    {
      "experienceId": "s09-exp-03",
      "title": "Self-Guided: Four-Question Test Suite Audit",
      "experienceType": "self-guided-exercise",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 3,
        "ambiguity": 4
      },
      "estimatedDurationMinutes": 25,
      "linkedCompetencies": ["testing-architecture", "architectural-judgment"],
      "desiredCognitiveActions": ["analysis", "reflection", "synthesis"],
      "contentBlocks": [],
      "interactionSequence": [],
      "workedExamples": [],
      "selfGuidedExercise": {
        "exerciseId": "s09-sge-01",
        "realWorldContext": "A feature in a codebase you work with that has existing tests — ideally a feature with at least 5 tests including at least one snapshot test or getByTestId usage.",
        "objective": "Apply the four-question audit framework to a real test suite, identify the weakest tests, rewrite them with RTL + MSW + userEvent, and verify the rewrites survive a superficial refactor.",
        "instructions": [
          "Find a feature test file in a codebase you work with that has at least 5 tests. Good candidates: a list component test, a form test, a dashboard widget test.",
          "Apply the four-question audit to each test: (1) Does it test behavior or implementation? (2) Does it verify behavior that could realistically regress? (3) Would it catch the bug it's supposedly preventing? (4) What is its false-positive rate on correct refactors?",
          "Identify the weakest two tests — those that score poorly on the most questions. Write down exactly why each fails.",
          "Rewrite both tests using RTL + MSW + userEvent. Rules: use getByRole as the primary query strategy, use userEvent for all interactions, use findBy for all async assertions, and use MSW for all network mocking (no direct fetch or Axios mocking).",
          "After rewriting, perform a superficial refactor: rename a CSS class, extract a sub-component, change a data-testid attribute. Verify the rewritten tests still pass.",
          "Write a one-paragraph test strategy recommendation for the feature: what the current suite gets right, what the gaps are, and what two specific changes would most improve confidence."
        ],
        "estimatedRealWorldDurationMinutes": 90,
        "materialsOrPrerequisites": [
          "A React codebase with at least one feature test file containing 5+ tests",
          "React Testing Library and ideally MSW installed, or willingness to write the rewrites as skeleton tests"
        ],
        "reflectionPrompts": [
          "After applying the four questions: what was the most common failure mode in the tests you audited — testing implementation, low regression risk, or high false-positive rate?",
          "After the superficial refactor: did any of your rewritten tests break? If yes, does the breakage indicate a real regression or a brittleness — and what would you change?",
          "Did adding MSW require more or less setup than the mocking approach in the original tests? What does the answer tell you about MSW's architectural position?",
          "After this audit, what is the single highest-value change to make to the test suite — and what behavioral regression would it newly catch?"
        ],
        "verification": {
          "mode": "reflection-submission",
          "isGating": false,
          "selfAssessmentCriteria": [
            "I applied all four audit questions to at least 5 tests and recorded specific verdicts",
            "I identified the two weakest tests with explicit reasoning",
            "I rewrote both tests using getByRole, userEvent, findBy, and MSW (or wrote skeleton tests with these patterns)",
            "I performed a superficial refactor and verified the rewrites survived",
            "I wrote a one-paragraph strategy recommendation with two specific improvements"
          ]
        },
        "linkedCompetencies": ["testing-architecture", "architectural-judgment"]
      },
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-testing-architecture"],
        "requiredPerformance": {
          "minimumAccuracy": 0.82,
          "minimumTransferScore": 0.80
        }
      }
    }
  ]
}
