{
  "sectionId": "s06",
  "title": "Component Architecture: Composition Models, Design Patterns & Accessibility",
  "description": "Covers the full palette of React composition patterns — compound components, headless components, HOCs, render props, the container/presentational split, and Portals — with explicit selection criteria, accessibility implications, and the renderHook testing approach for behavioral contracts.",
  "learningObjectives": [
    {
      "objectiveId": "s06-obj-01",
      "description": "Implement a compound component pattern for a UI widget that requires flexible internal composition.",
      "linkedCompetencies": ["component-design-patterns", "functional-programming-react"]
    },
    {
      "objectiveId": "s06-obj-02",
      "description": "Build a headless component that separates behavior from visual presentation, explain its testability advantage, and identify how it serves as an accessibility architecture tool.",
      "linkedCompetencies": ["component-design-patterns", "testing-architecture"]
    },
    {
      "objectiveId": "s06-obj-03",
      "description": "Evaluate a given component design and select the most appropriate pattern (HOC, render prop, compound, or hook extraction), defending the choice against alternatives.",
      "linkedCompetencies": ["component-design-patterns", "architectural-judgment"]
    },
    {
      "objectiveId": "s06-obj-04",
      "description": "Refactor a monolithic component into a container/logic + presentational split using custom hooks, preserving type contracts.",
      "linkedCompetencies": ["component-design-patterns", "typescript-react-contracts"]
    },
    {
      "objectiveId": "s06-obj-05",
      "description": "Implement a React Portal for a modal or toast notification and explain why Portal placement matters for stacking context, focus management, and accessibility.",
      "linkedCompetencies": ["component-design-patterns"]
    },
    {
      "objectiveId": "s06-obj-06",
      "description": "Design a custom hook as a complete behavioral contract, write a renderHook test for it, and explain how the hook's interface should be stable regardless of its internal implementation.",
      "linkedCompetencies": ["component-design-patterns", "testing-architecture"]
    }
  ],
  "motivationHooks": [
    {
      "type": "real-world-stakes",
      "prompt": "Component design is where architectural intent becomes code. The same feature can be built as a monolithic component, a compound component, a headless component, or an HOC-wrapped component — and each choice has a different maintenance cost, testability profile, and accessibility story. Knowing which pattern fits which constraint, and being able to defend that choice, is what separates a React architect from a React developer."
    },
    {
      "type": "prediction-gap",
      "prompt": "Before reading: a client needs a Select component that behaves identically on desktop (dropdown) and mobile (bottom sheet) — same keyboard navigation, same ARIA, completely different markup. Which React pattern best fits this requirement? And separately: a Modal rendered inside a parent with transform: translateZ(0) appears offset and ignores z-index. Why, and what is the fix? Hold both answers — they reveal the architectural purpose behind two patterns most developers treat as implementation details."
    }
  ],
  "furtherReading": [
    {
      "resourceId": "s06-fr-01",
      "title": "Compound Components Pattern — Kent C. Dodds",
      "resourceType": "article",
      "url": "https://kentcdodds.com/blog/compound-components-with-react-hooks",
      "author": "Kent C. Dodds",
      "rationaleNote": "The definitive practical guide to compound components with hooks. Covers the Context sharing model, the static property attachment pattern (Tabs.Tab = TabsTab), and the error guard convention. Read after the section to see an alternative implementation approach.",
      "linkedCompetencies": ["component-design-patterns"]
    },
    {
      "resourceId": "s06-fr-02",
      "title": "Radix UI — Unstyled, Accessible Components",
      "resourceType": "website",
      "url": "https://www.radix-ui.com/",
      "author": "Radix UI Team",
      "rationaleNote": "The leading production headless component library. Browsing Radix's source and API design is the fastest way to understand prop getters, keyboard navigation contracts, and ARIA encapsulation at scale. Compare their Dialog API to what you'd need to implement manually.",
      "linkedCompetencies": ["component-design-patterns"]
    },
    {
      "resourceId": "s06-fr-03",
      "title": "ARIA Authoring Practices Guide — Dialog Pattern",
      "resourceType": "website",
      "url": "https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/",
      "author": "W3C WAI",
      "rationaleNote": "The authoritative ARIA specification for modal dialogs — focus trap, role='dialog', aria-modal, focus restoration, and Escape key handling. The accessibility requirements in Part 5 are drawn directly from this spec. Non-negotiable reading for anyone implementing modals.",
      "linkedCompetencies": ["component-design-patterns"]
    },
    {
      "resourceId": "s06-fr-04",
      "title": "React Testing Library: renderHook",
      "resourceType": "website",
      "url": "https://testing-library.com/docs/react-testing-library/api/#renderhook",
      "author": "Testing Library Team",
      "rationaleNote": "The official API reference for renderHook. Covers the act() wrapper requirement for state updates, the result.current pattern, and how to test hooks that depend on React context. Directly supports the renderHook test examples in Part 6.",
      "linkedCompetencies": ["component-design-patterns", "testing-architecture"]
    }
  ],
  "learningExperiences": [
    {
      "experienceId": "s06-exp-01",
      "title": "Component Architecture Patterns: From Compound to Headless to Portal",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 4,
        "ambiguity": 4
      },
      "estimatedDurationMinutes": 45,
      "linkedCompetencies": ["component-design-patterns", "functional-programming-react", "testing-architecture", "typescript-react-contracts"],
      "desiredCognitiveActions": ["prediction", "analysis", "comparison", "synthesis", "retrieval"],
      "contentBlocks": [
        {
          "blockId": "s06-cb-01",
          "type": "text",
          "content": "WHY THIS MATTERS\n\nComponent design is where architectural intent becomes code. The same feature can be built as a monolithic component, a compound component, a headless component, or an HOC-wrapped component — and each choice has a different maintenance cost, testability profile, and accessibility story. Knowing which pattern fits which constraint, and being able to defend that choice, is what separates a React architect from a React developer."
        },
        {
          "blockId": "s06-cb-02",
          "type": "text",
          "content": "LEARNING OBJECTIVES\n\nBy the end of this section, you will be able to:\n1. Implement a compound component pattern for a UI widget requiring flexible internal composition.\n2. Build a headless component separating behavior from visual presentation, and explain its testability and accessibility advantages.\n3. Evaluate a component design and select the most appropriate pattern, defending it against alternatives.\n4. Refactor a monolithic component into a container/logic + presentational split using custom hooks.\n5. Implement a React Portal for a modal or toast and explain why placement matters for stacking context and accessibility.\n6. Design a custom hook as a complete behavioral contract and write a renderHook test for it."
        },
        {
          "blockId": "s06-cb-03",
          "type": "text",
          "content": "KEY TERMS & DEFINITIONS\n\nCompound component — A set of components that share implicit state via Context, designed to be composed by the consumer. <Select> + <Select.Option> is the canonical example. The consumer controls layout; the parent controls state.\n\nHeadless component — A component or hook that provides behavior and state with no opinions about rendering. The consumer owns all markup and styles. Radix UI and Headless UI are library examples.\n\nHigher-order component (HOC) — A function that takes a component and returns a new component with additional behavior injected. withAuth(Dashboard) is a HOC.\n\nRender prop — A pattern where a component receives a function as a prop and calls it to produce its output, giving the consumer control over rendering. Largely superseded by hooks, but still valid for certain cross-cutting concerns.\n\nContainer/presentational split — Separating a component's data-fetching and logic (container) from its pure rendering (presentational). In modern React, the container is usually a custom hook rather than a wrapper component.\n\nPortal — A React feature that renders a component's output into a different DOM node than its parent. Used for modals, tooltips, and toasts that must escape CSS stacking contexts.\n\nStacking context — A CSS concept where a positioned element creates an isolated rendering layer. overflow: hidden, transform, opacity < 1, and z-index on positioned elements all create new stacking contexts.\n\nBehavioral contract — The public interface of a custom hook: what it accepts, what it returns, and what lifecycle guarantees it makes — independent of its implementation."
        },
        {
          "blockId": "s06-cb-04",
          "type": "text",
          "content": "PART 1: COMPOUND COMPONENTS — FLEXIBLE COMPOSITION\n\nThe compound component pattern solves a specific problem: a widget with multiple internal parts where consumers need layout flexibility, but the parts need to share state.\n\nTHE NAIVE APPROACH passes everything as props:\n\n  // Inflexible — consumer can't reorder or add content between items\n  <Tabs\n    tabs={['Overview', 'Settings', 'Billing']}\n    activeTab={activeTab}\n    onTabChange={setActiveTab}\n    content={tabContent}\n  />\n\nTHE COMPOUND COMPONENT APPROACH shares state via Context:\n\n  // Consumer controls layout; Tabs controls state\n  <Tabs defaultTab=\"overview\">\n    <Tabs.List>\n      <Tabs.Tab id=\"overview\">Overview</Tabs.Tab>\n      <Tabs.Tab id=\"settings\">Settings</Tabs.Tab>\n      {canSeeBilling && <Tabs.Tab id=\"billing\">Billing</Tabs.Tab>}\n    </Tabs.List>\n    <Tabs.Panel id=\"overview\"><OverviewContent /></Tabs.Panel>\n    <Tabs.Panel id=\"settings\"><SettingsContent /></Tabs.Panel>\n    {canSeeBilling && <Tabs.Panel id=\"billing\"><BillingContent /></Tabs.Panel>}\n  </Tabs>\n\nIMPLEMENTATION — the key pieces:\n\n  const TabsContext = createContext<TabsContextValue | null>(null);\n\n  function useTabs() {\n    const ctx = useContext(TabsContext);\n    if (!ctx) throw new Error('useTabs must be used within <Tabs>');\n    return ctx;\n  }\n\n  function Tabs({ defaultTab, children }) {\n    const [activeTab, setActiveTab] = useState(defaultTab);\n    return (\n      <TabsContext.Provider value={{ activeTab, setActiveTab }}>\n        {children}\n      </TabsContext.Provider>\n    );\n  }\n\n  function TabsTab({ id, children }) {\n    const { activeTab, setActiveTab } = useTabs();\n    return (\n      <button role=\"tab\" aria-selected={activeTab === id} onClick={() => setActiveTab(id)}>\n        {children}\n      </button>\n    );\n  }\n\n  Tabs.Tab   = TabsTab;\n  Tabs.List  = TabsList;\n  Tabs.Panel = TabsPanel;\n\nACCESSIBILITY NOTE: Compound components are where ARIA roles naturally attach. role='tab', role='tablist', role='tabpanel', and aria-selected belong at the compound component level — the consumer shouldn't need to wire these up manually. Encapsulating ARIA into the compound structure is a key accessibility architecture advantage.\n\nWHEN TO USE: When a widget has multiple coordinated sub-parts and consumers need layout flexibility. Tabs, Accordions, Dropdowns, Menus, and multi-step forms are canonical use cases.\n\nWHEN NOT TO USE: When consumers don't actually need layout control. If the internal layout is always the same, props are simpler and less indirection."
        },
        {
          "blockId": "s06-cb-05",
          "type": "text",
          "content": "PART 2: HEADLESS COMPONENTS — BEHAVIOR WITHOUT PRESENTATION\n\nA headless component provides behavior, state, and accessibility — zero markup, zero styles. The consumer owns the entire visual layer.\n\nWHY IT MATTERS ARCHITECTURALLY:\n1. Testability — behavior is testable without rendering any specific UI. Tests don't break when styling changes.\n2. Accessibility — ARIA attributes, keyboard handlers, and focus management live in the headless layer, shared across every visual implementation.\n3. Design flexibility — the same behavior can be applied to a button-group tab bar, a dropdown tab bar, or a mobile swipe interface.\n\nHOOK-BASED HEADLESS PATTERN:\n\n  function useDisclosure(initialOpen = false) {\n    const [isOpen, setIsOpen] = useState(initialOpen);\n    return {\n      isOpen,\n      open:   () => setIsOpen(true),\n      close:  () => setIsOpen(false),\n      toggle: () => setIsOpen(o => !o),\n      getTriggerProps: () => ({\n        onClick: () => setIsOpen(o => !o),\n        'aria-expanded': isOpen,\n      }),\n      getContentProps: () => ({\n        hidden: !isOpen,\n        role: 'region' as const,\n      }),\n    };\n  }\n\n  // Consumer owns all markup and styling\n  function CustomAccordion({ title, children }) {\n    const { getTriggerProps, getContentProps } = useDisclosure();\n    return (\n      <div className=\"my-accordion-style\">\n        <button className=\"my-trigger-style\" {...getTriggerProps()}>{title}</button>\n        <div className=\"my-content-style\" {...getContentProps()}>{children}</div>\n      </div>\n    );\n  }\n\nPROP GETTER PATTERN: Returning pre-built prop objects (getTriggerProps, getMenuProps) is the headless hook convention. It encapsulates ARIA attributes and event handlers; consumers spread them onto whatever element they choose. This is the pattern used by Downshift, Headless UI, and Radix."
        },
        {
          "blockId": "s06-cb-06",
          "type": "text",
          "content": "PART 3: HOCs AND RENDER PROPS — WHEN THEY STILL APPLY\n\nHooks replaced many HOC and render prop use cases, but not all.\n\nHOCs STILL MAKE SENSE FOR:\n- Cross-cutting injection at the route or page level: withAuth, withErrorBoundary, withAnalytics.\n- Third-party library integration that provides a component wrapper API.\n- Legacy codebase compatibility where refactoring to hooks would be large and risky.\n\n  function withAuth<P extends object>(Component: ComponentType<P>) {\n    return function AuthGuard(props: P) {\n      const { isAuthenticated, isLoading } = useAuth();\n      if (isLoading) return <LoadingScreen />;\n      if (!isAuthenticated) return <Navigate to=\"/login\" />;\n      return <Component {...props} />;\n    };\n  }\n\n  const ProtectedDashboard = withAuth(Dashboard);\n\nTHE HOC WARNING: HOCs obscure the component tree in DevTools and can create prop collision bugs when multiple HOCs wrap the same component. Prefer hooks for anything that doesn't require wrapping the entire render output.\n\nRENDER PROPS STILL MAKE SENSE FOR:\n- Exposing render control when a hook's return value isn't enough.\n- Virtualization libraries (like TanStack Virtual) that control how a list is rendered.\n\n  <VirtualList\n    items={rows}\n    renderItem={(item, style) => (\n      <div style={style}><RowComponent data={item} /></div>\n    )}\n  />"
        },
        {
          "blockId": "s06-cb-07",
          "type": "text",
          "content": "PART 4: CONTAINER/PRESENTATIONAL SPLIT WITH CUSTOM HOOKS\n\nThe original container/presentational pattern used wrapper components. In modern React, the container is a custom hook.\n\n  // Old pattern — wrapper component as container\n  function UserProfileContainer({ userId }) {\n    const { data, isLoading } = useUser(userId);\n    const permissions = usePermissions();\n    if (isLoading) return <Skeleton />;\n    return <UserProfilePresentation user={data} permissions={permissions} />;\n  }\n\n  // Modern pattern — custom hook as container\n  function useUserProfileData(userId: UserId) {\n    const user        = useUser(userId);        // suspense-compatible\n    const permissions = usePermissions();\n    return { user, permissions };\n  }\n\n  // Component is purely presentational — fully testable with props alone\n  function UserProfile({ userId }: { userId: UserId }) {\n    const { user, permissions } = useUserProfileData(userId);\n    return (\n      <div>\n        <UserAvatar user={user} />\n        {permissions.canEdit && <EditButton />}\n      </div>\n    );\n  }\n\nTHE TESTABILITY ADVANTAGE: UserProfile can now be tested as a pure presentational component by passing props directly — no need to mock API calls. useUserProfileData can be tested with renderHook. The split creates two independently testable units."
        },
        {
          "blockId": "s06-cb-08",
          "type": "text",
          "content": "PART 5: PORTALS — ESCAPING THE DOM HIERARCHY\n\nReact Portals render a component's output into a different DOM node, while keeping it in the React component tree (so Context, events, and error boundaries all work normally).\n\nWHY PORTALS ARE NECESSARY:\nA modal rendered inside a <div> with overflow: hidden or transform applied will be clipped or incorrectly positioned regardless of its z-index. The CSS stacking context created by that parent cannot be escaped by CSS alone — only by physically placing the DOM node outside the hierarchy.\n\n  function Modal({ children, onClose }: ModalProps) {\n    return createPortal(\n      <div\n        role=\"dialog\"\n        aria-modal=\"true\"\n        className=\"modal-overlay\"\n        onClick={onClose}\n      >\n        <div\n          className=\"modal-content\"\n          onClick={e => e.stopPropagation()}\n        >\n          {children}\n        </div>\n      </div>,\n      document.body  // renders outside any stacking context\n    );\n  }\n\nACCESSIBILITY REQUIREMENTS FOR MODALS (non-negotiable):\n- role='dialog' and aria-modal='true' on the dialog container.\n- aria-labelledby pointing to the modal's heading.\n- Focus trap — keyboard focus must stay inside the modal while it's open.\n- Focus restoration — when the modal closes, focus returns to the element that triggered it.\n- Escape key closes the modal.\n\nThese requirements belong at the Portal/Modal component level — consumers shouldn't have to implement them manually."
        },
        {
          "blockId": "s06-cb-09",
          "type": "text",
          "content": "PART 6: CUSTOM HOOK AS BEHAVIORAL CONTRACT — renderHook IN PRACTICE\n\nSection 4 introduced the hook contract concept. Here it's applied concretely: designing a hook with a stable public interface, then verifying it with renderHook.\n\n  // Contract: manages a multi-step form's step progression\n  function useMultiStepForm(totalSteps: number) {\n    const [currentStep, setCurrentStep] = useState(0);\n    return {\n      currentStep,\n      totalSteps,\n      isFirstStep: currentStep === 0,\n      isLastStep:  currentStep === totalSteps - 1,\n      next:  () => setCurrentStep(s => Math.min(s + 1, totalSteps - 1)),\n      back:  () => setCurrentStep(s => Math.max(s - 1, 0)),\n      goTo:  (step: number) => setCurrentStep(Math.max(0, Math.min(step, totalSteps - 1))),\n      reset: () => setCurrentStep(0),\n    };\n  }\n\nrenderHook TESTS — test the contract, not the implementation:\n\n  describe('useMultiStepForm', () => {\n    it('starts at step 0', () => {\n      const { result } = renderHook(() => useMultiStepForm(3));\n      expect(result.current.currentStep).toBe(0);\n      expect(result.current.isFirstStep).toBe(true);\n    });\n\n    it('advances to next step', () => {\n      const { result } = renderHook(() => useMultiStepForm(3));\n      act(() => result.current.next());\n      expect(result.current.currentStep).toBe(1);\n    });\n\n    it('does not advance past the last step', () => {\n      const { result } = renderHook(() => useMultiStepForm(3));\n      act(() => { result.current.next(); result.current.next(); result.current.next(); });\n      expect(result.current.currentStep).toBe(2);\n      expect(result.current.isLastStep).toBe(true);\n    });\n  });\n\nThe tests make no assumptions about how currentStep is stored internally. If you replace useState with useReducer, all tests still pass — the contract is stable."
        },
        {
          "blockId": "s06-cb-10",
          "type": "text",
          "content": "COMMON ERRORS\n\nError 1: Compound components without Context error guards\nIf <Tabs.Tab> can be rendered outside <Tabs>, it will silently receive undefined context and fail in a confusing way. Always throw a descriptive error in the consuming hook: if (!ctx) throw new Error('<Tabs.Tab> must be used within <Tabs>').\n\nError 2: Headless hooks that leak internal state\nReturning raw setActiveIndex from a headless hook gives consumers the power to put it in invalid states. Return prop getters and named action functions only — never raw setters.\n\nError 3: Portals without focus management\nA modal that doesn't trap focus lets keyboard users tab out of it into background content — a critical accessibility failure. WCAG 2.1 criterion 2.1.2 and the ARIA modal pattern both require focus containment. This is not optional.\n\nError 4: HOC stacking\nwithAuth(withAnalytics(withErrorBoundary(Dashboard))) creates a deeply nested component tree, makes DevTools debugging painful, and can cause prop collisions. Prefer a single HOC that composes the concerns, or replace with hooks where possible.\n\nError 5: Premature abstraction into compound components\nIf a widget's internal layout never changes, a compound component adds indirection with no flexibility benefit. Abstract when you have two concrete use cases with genuinely different layout requirements, not speculatively."
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s06-int-01",
          "interactionType": "prediction",
          "prompt": "Before reading: a client needs a Select component that behaves identically on desktop (a dropdown list) and mobile (a bottom sheet) — same keyboard navigation, same ARIA attributes, completely different DOM structure and styles. Which pattern best fits this requirement?\n\nA) Compound component — share state between Select.Trigger and Select.Options sub-components.\nB) Headless hook with prop getters — separate behavior entirely from markup so each platform can own its own DOM structure.\nC) HOC — wrap a shared Select component with different visual wrappers per platform.\nD) Render prop — pass the rendering function as a prop so the consumer controls the output.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct — a headless hook with prop getters. The key constraint is 'completely different DOM structure' per platform. Compound components (A) still impose a component hierarchy with specific sub-components — they control the layout structure even if they're flexible. An HOC (C) wraps the same component, so the underlying DOM structure is shared. A render prop (D) is closer, but prop getters from a headless hook are the modern, more composable approach: the hook returns getTriggerProps(), getListboxProps(), getItemProps(index) — the consumer spreads these onto whatever elements they choose. The desktop implementation uses a <div> + <ul>; the mobile implementation uses a <Sheet> + <SwipeableList> — but both use the same behavior hook.",
            "correctiveFeedback": "B: headless hook. The constraint is 'completely different DOM structure' — compound components still control structure. The headless hook provides behavior (keyboard nav, ARIA, selection) and the consumer owns 100% of the markup.",
            "hints": ["Which pattern makes the fewest assumptions about what DOM elements the consumer will use?", "Could a compound component's Tab.Item work as a SwipeableListItem on mobile?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in your answer? (1 = guessing, 5 = certain)"
          }
        },
        {
          "interactionId": "s06-int-02",
          "interactionType": "multiple-choice",
          "prompt": "A <Menu.Item> compound sub-component is accidentally rendered outside any <Menu> parent. What is the correct defensive behavior?\n\nA) Render with default props silently.\nB) Return null to avoid a crash.\nC) Throw a descriptive error identifying that <Menu.Item> requires a <Menu> ancestor.\nD) Log a console warning and render with fallback styles.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "C is correct. An explicit thrown error with a descriptive message is always preferable to silent failure. Options A and D allow the component to render in an undefined state, creating confusing bugs that may not surface until runtime in a completely different context. Option B (returning null) also fails silently — the developer sees nothing in the UI and has to trace back through the tree to find the problem. A thrown error in the useTabs() consuming hook — 'if (!ctx) throw new Error(\"<Tabs.Tab> must be used within <Tabs>\")' — fails immediately and loudly in development, making the misuse obvious before it reaches production.",
            "correctiveFeedback": "C: throw a descriptive error. Silent failures (null, fallback styles, default props) hide misuse. An explicit throw in the context consumer hook surfaces the problem immediately in development.",
            "hints": ["Which approach makes the misuse visible immediately vs. creating a confusing runtime bug?", "What does returning null look like from the developer's perspective?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in this answer?"
          }
        },
        {
          "interactionId": "s06-int-03",
          "interactionType": "multiple-choice",
          "prompt": "Which of these is a correct reason to use a React Portal?\n\nA) To share state between two sibling components without prop drilling.\nB) To render a modal outside a parent element that has overflow: hidden applied.\nC) To lazy-load a component only when it's first needed.\nD) To prevent a component from re-rendering when its parent re-renders.",
          "required": true,
          "responseFormat": "multiple-choice",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "B is correct. Portals render a component's DOM output into a different node, physically moving it outside the ancestor tree that contains the stacking context. overflow: hidden and transform create CSS stacking contexts — any absolutely or fixed positioned child within that context is clipped or positioned relative to the stacking context root, regardless of z-index. Moving the modal's DOM node to document.body via a Portal escapes this constraint entirely. Importantly, the component remains in the React tree — Context, event bubbling, and Error Boundaries all still work as if the Portal were rendered in its original position. Options A, C, and D describe completely different features (Context/composition, React.lazy, and React.memo).",
            "correctiveFeedback": "B: Portals escape CSS stacking contexts by physically placing the DOM output in a different node (usually document.body). The component stays in the React tree — only the DOM output moves.",
            "hints": ["What does overflow: hidden do to absolutely positioned children?", "Does a Portal change where the component sits in the React tree?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How confident are you in this answer?"
          }
        },
        {
          "interactionId": "s06-int-04",
          "interactionType": "scenario-analysis",
          "prompt": "A useDropdown hook currently returns: { isOpen, setIsOpen, activeIndex, setActiveIndex }. A developer argues: 'This is fine — the consumer has everything they need.' Apply the hook contract test and the prop getter principle to evaluate this design. What specifically is wrong, and what should the hook return instead?",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "Contract test failures:\n1. Can you describe it in one sentence without mentioning internals? Marginally — but the interface implies consumers know about setIsOpen and setActiveIndex, which are implementation details.\n2. Replaceable internals? No — if the hook internally switches to useReducer, setIsOpen and setActiveIndex no longer exist; every call site breaks.\n3. Testable without mocking internal structure? No — renderHook tests would call setActiveIndex(-1), which is an invalid state the hook should protect against.\n\nProp getter principle violation: raw setters expose internal state management. A consumer calling setActiveIndex(-1) or setIsOpen(true) while activeIndex is out of bounds for the current items can put the hook in invalid states it doesn't handle.\n\nCorrect return interface:\n  {\n    isOpen, // derived state values are fine to expose\n    activeIndex,\n    getTriggerProps: () => ({ onClick: toggle, 'aria-expanded': isOpen }),\n    getMenuProps:    () => ({ role: 'listbox' }),\n    getItemProps:    (index) => ({ 'aria-selected': index === activeIndex, onClick: () => select(index) }),\n    open, close, toggle,  // named action functions, not raw setters\n  }\n\nThe consumer spreads prop getters onto their elements — they never call setIsOpen or setActiveIndex directly.",
            "correctiveFeedback": "The hook fails all three contract questions: setter exposure breaks replaceability and testability, and allows consumers to create invalid states. Replace raw setters with named action functions (open/close/toggle/select) and prop getters that bundle ARIA + event handlers.",
            "hints": ["What happens to a call site that uses setIsOpen if the implementation switches to useReducer?", "Could a consumer call setActiveIndex(-1) with the current interface? Should they be able to?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How thoroughly did you apply both the contract test and the prop getter principle?"
          }
        },
        {
          "interactionId": "s06-int-05",
          "interactionType": "reflection",
          "prompt": "REINFORCING SECTIONS 4 & 5 — connecting component patterns to FP principles and state topology (bridges ig-state-components group): The compound component Tabs uses an internal Context to share activeTab state between Tabs.Tab and Tabs.Panel. From Section 5 you know Context is a DI mechanism, not a state manager. From Section 4 you know HOFs compose behavior. In 2-3 sentences: explain why Context is the architecturally correct mechanism for compound component internal state specifically — and why this does NOT create the 'Context as state manager' performance problem from Section 5.",
          "required": true,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "Context is correct here because compound component internal state (activeTab) is scoped to a tightly bounded subtree — the Tabs component and its direct children. This is exactly the 'tightly scoped feature subtree' use case from Section 5's 'when Context is the right tool' list.\n\nThe Section 5 performance problem occurs when Context is used for high-frequency global state that causes many unrelated components across the app to re-render. The Tabs Context is different: (1) it's scoped — only Tabs.Tab and Tabs.Panel consume it, (2) the consumer count is small and bounded by the Tabs instance, and (3) when activeTab changes, only the Tabs sub-components re-render — which is correct behavior since their visual output depends on it.\n\nThe FP connection from Section 4: compound components are HOFs at the component level — Tabs takes children (behavior inputs) and wraps them with shared state (behavior output). The Context is the composition mechanism that makes the HOF pattern work across a JSX tree.",
            "correctiveFeedback": "Context is correct for compound internal state because it's scoped to a small, bounded subtree with a small, bounded consumer set. The Section 5 problem is global Context with high-frequency updates and many unrelated consumers — neither applies here.",
            "hints": ["How many components consume the Tabs Context vs. an AppContext?", "When activeTab changes, should Tabs.Tab re-render? Is that a performance problem?"]
          },
          "confidencePrompt": {
            "enabled": true,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How well did you connect the compound component Context to both the Section 5 DI principle and the Section 4 HOF model?"
          }
        }
      ],
      "workedExamples": [
        {
          "exampleId": "s06-we-01",
          "problem": "A command palette (⌘K menu) needs: opening from anywhere in the app, a searchable list of keyboard-navigable actions, visual customizability per product area, and escape from CSS stacking contexts. Select the patterns, justify each choice, and sketch the architecture.",
          "steps": [
            "Step 1 — Identify the constraints. Open state is global (triggered from anywhere in the app) → not a compound component concern; requires cross-cutting state. Keyboard navigation + ARIA + search filtering = complex behavior that must survive visual redesign → headless hook. Visual customizability per product area → behavior must be decoupled from rendering. Must escape stacking contexts → Portal.",
            "Step 2 — Select patterns and justify each. useCommandPalette() → headless hook. Owns keyboard nav (↑↓ Enter Escape), search filtering, active index tracking, and all ARIA prop getters. Decoupled from rendering means visual redesigns don't touch the hook. <CommandPalettePortal> → Portal rendered into document.body. Escapes any transform or overflow:hidden stacking contexts in the app shell. CommandPalette.Input + CommandPalette.List + CommandPalette.Item → compound components. Consumers control layout (input above list, list in sidebar, etc.); the compound structure controls wiring. Global open state → Zustand. Cross-cutting: a keyboard shortcut anywhere in the app triggers it; the command palette component reads the store.",
            "Step 3 — Sketch the useCommandPalette hook interface. Accepts commands[]. Returns: query (current search), filtered (matching commands), activeIndex, getInputProps() { value, onChange, onKeyDown }, getItemProps(index) { aria-selected, onClick }, getListboxProps() { role: 'listbox' }. No markup, no styles, no opinions about DOM structure.",
            "Step 4 — Verify the pattern selection. Each pattern is selected for a concrete, distinct reason. The headless hook cannot own DOM placement (Portal's job). The Portal cannot own keyboard behavior (hook's job). The compound components cannot own cross-cutting open state (Zustand's job). No pattern is doing another's job."
          ],
          "conclusion": "Each pattern is selected for a concrete reason — not as a default or habit. The headless hook owns behavior. The Portal owns DOM placement. Compound components own layout flexibility. Zustand owns cross-cutting state. When you can give each pattern a one-sentence justification tied to a specific constraint, the architecture is sound."
        }
      ],
      "retrievalPractice": [
        {
          "retrievalItemId": "s06-retrieval-01",
          "retrievalType": "cued-recall",
          "prompt": "What specific problem does the compound component pattern solve that props alone cannot? Give one concrete example of a use case that requires compound components and one that does not.",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [3, 7, 14, 30],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s06-retrieval-02",
          "retrievalType": "free-recall",
          "prompt": "Name the five accessibility requirements that belong at the Portal/Modal component level. For each, explain what breaks if the consumer has to implement it manually instead.",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 1,
            "subsequentReviewDays": [4, 10, 21],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 1,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s06-retrieval-03",
          "retrievalType": "contrast",
          "prompt": "Contrast a headless hook's prop getter return (e.g. getTriggerProps()) with returning raw state values and setters. What does each approach allow or prevent the consumer from doing? Which correctly enforces the behavioral contract?",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [5, 14, 30],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s06-retrieval-04",
          "retrievalType": "application",
          "prompt": "What is the modern equivalent of the container component in the hooks era? Show the structure: a custom hook that fetches data + a presentational component that renders it. What does each unit test independently, and how?",
          "difficulty": 2,
          "schedule": {
            "initialReviewDays": 2,
            "subsequentReviewDays": [6, 14, 30],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.1,
              "minimumIntervalDays": 2,
              "maximumIntervalDays": 45
            }
          }
        },
        {
          "retrievalItemId": "s06-retrieval-05",
          "retrievalType": "application",
          "prompt": "When does the HOC pattern still make sense over a custom hook? Give two concrete use cases where HOC is the correct choice, and explain what specifically about those cases makes a hook insufficient.",
          "difficulty": 3,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [7, 21, 45],
            "decayParameters": {
              "decayRate": 0.3,
              "strengthMultiplier": 2.0,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        },
        {
          "retrievalItemId": "s06-retrieval-06",
          "retrievalType": "scenario-analysis",
          "prompt": "INTERLEAVING — connecting component patterns to state topology and testing architecture (bridges ig-state-components and ig-testing-performance groups, reinforces S05 and previews S09): A headless useDropdown hook is tested with renderHook. The test calls result.current.open() and asserts result.current.isOpen === true. A developer later changes the internal implementation from useState to useReducer. Do the tests break? Now apply the Section 5 state classification framework: what kind of state is isOpen? Why does that classification tell you it belongs inside the hook rather than in a global store?",
          "difficulty": 4,
          "schedule": {
            "initialReviewDays": 3,
            "subsequentReviewDays": [8, 21, 45],
            "decayParameters": {
              "decayRate": 0.28,
              "strengthMultiplier": 2.2,
              "minimumIntervalDays": 3,
              "maximumIntervalDays": 60
            }
          }
        }
      ],
      "transferExercises": [
        {
          "exerciseId": "s06-transfer-01",
          "context": "Logistics analytics data visualization dashboard",
          "scenario": "A logistics analytics tool needs three UI features:\n1. A chart type selector (bar, line, scatter) that appears as a radio button group on desktop and a bottom sheet on mobile — same behavior, different presentation.\n2. A 'drill-down' overlay showing detail data when a chart segment is clicked — must escape the chart's SVG/canvas stacking context.\n3. A filter panel with nested region → country → city selectors that share selection state.",
          "task": "1. For the chart type selector: which pattern best fits the 'same behavior, different presentation' requirement? Sketch the hook's public interface — what does it accept, what prop getters does it return?\n2. For the drill-down overlay: why is a Portal required here specifically? What would go wrong without one (SVG/canvas creates a stacking context)?\n3. For the filter panel: is this a compound component use case, a Context use case, or both? Draw the component tree and mark where state lives.\n4. Write a renderHook test outline for the chart type selector hook that verifies the behavioral contract without depending on visual implementation.",
          "successCriteria": [
            "Chart type selector: headless hook — same behavior, different DOM. Interface: useChartTypeSelector(options) returns { selectedType, getOptionProps(type) { aria-checked, onClick } }, getGroupProps() { role: 'radiogroup' }. No mention of DOM elements in the hook.",
            "Drill-down overlay: Portal required because SVG/canvas elements create their own stacking context — any absolutely positioned child overlay is clipped to the SVG/canvas bounds. A Portal to document.body escapes this.",
            "Filter panel: both compound component AND Context. Compound components (Filter.RegionSelect, Filter.CountrySelect, Filter.CitySelect) provide layout flexibility. An internal Context shares the full filter selection state between all three levels — country options depend on selected region; city options depend on selected country.",
            "renderHook test outline: renders useChartTypeSelector(['bar', 'line', 'scatter']); asserts initial selectedType is 'bar'; calls getOptionProps('line').onClick(); asserts selectedType is now 'line'. No assertion on DOM structure or CSS classes."
          ]
        }
      ],
      "masteryVerification": {
        "assessmentIds": ["assess-component-patterns"],
        "requiredPerformance": {
          "minimumAccuracy": 0.85,
          "minimumTransferScore": 0.80
        }
      }
    },
    {
      "experienceId": "s06-exp-02",
      "title": "Pattern Identification and Refactor: Practical Activity",
      "experienceType": "guided-analysis",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 3,
        "ambiguity": 3
      },
      "estimatedDurationMinutes": 25,
      "linkedCompetencies": ["component-design-patterns", "testing-architecture"],
      "desiredCognitiveActions": ["analysis", "synthesis", "reflection"],
      "contentBlocks": [
        {
          "blockId": "s06-cb-11",
          "type": "text",
          "content": "PRACTICAL ACTIVITY: PATTERN IDENTIFICATION AND REFACTOR\n\nGiven this monolithic component:\n\n  function UserDropdown({ userId }: { userId: UserId }) {\n    const [open, setOpen] = useState(false);\n    const [activeIndex, setActiveIndex] = useState(0);\n    const user = useUser(userId);\n    const { logout } = useAuth();\n    const navigate = useNavigate();\n\n    const items = [\n      { label: 'Profile',  action: () => navigate('/profile') },\n      { label: 'Settings', action: () => navigate('/settings') },\n      { label: 'Log out',  action: logout },\n    ];\n\n    return (\n      <div>\n        <button onClick={() => setOpen(o => !o)} aria-expanded={open}>\n          <img src={user.avatarUrl} alt={user.name} />\n        </button>\n        {open && (\n          <ul role=\"menu\">\n            {items.map((item, i) => (\n              <li\n                key={item.label}\n                role=\"menuitem\"\n                aria-selected={i === activeIndex}\n                onClick={item.action}\n              >\n                {item.label}\n              </li>\n            ))}\n          </ul>\n        )}\n      </div>\n    );\n  }\n\nWork through the following:\n1. Extract the behavior (open state, active index, keyboard nav, ARIA props) into a useDropdown headless hook with prop getters.\n2. Extract the data fetching (useUser, useAuth, useNavigate) into a useUserDropdownData hook.\n3. Rewrite UserDropdown as a presentational component that composes both hooks.\n4. Write two renderHook tests for useDropdown: one for initial state, one verifying that open() sets isOpen to true."
        }
      ],
      "interactionSequence": [
        {
          "interactionId": "s06-int-06",
          "interactionType": "reflection",
          "prompt": "After completing the refactor: you now have useDropdown (behavior), useUserDropdownData (data), and UserDropdown (presentation). A designer wants to completely redesign the dropdown's visual appearance — new markup, new styles, new animation. Which of your three units needs to change? Which can stay exactly the same? What does this tell you about the architectural value of the separation?",
          "required": false,
          "responseFormat": "short-answer",
          "revealCondition": "after-response",
          "feedback": {
            "type": "elaborative",
            "elaborativeFeedback": "Only UserDropdown (the presentational layer) needs to change — new markup, new class names, new animation wrappers. useDropdown stays exactly the same: it owns behavior (open/close, keyboard nav, ARIA prop getters), and none of that changes for a visual redesign. useUserDropdownData stays exactly the same: it owns data fetching and navigation logic, which has nothing to do with visual design.\n\nThe architectural value: a visual redesign that previously would have required touching a 50-line monolithic component now touches only the presentational layer — with zero risk of accidentally breaking keyboard navigation or ARIA attributes, because those live in an independently tested hook. The renderHook tests for useDropdown serve as a regression safety net that survives the redesign unchanged.\n\nThis is the core payoff of the container/presentational + headless pattern: each axis of change (visual design, behavior, data) has its own locus. Changes are isolated. Risk is bounded.",
            "correctiveFeedback": "Only the presentational component changes. The headless hook and data hook are untouched — and their existing tests still pass. Each axis of change (visual, behavioral, data) has its own bounded locus.",
            "hints": ["Did the keyboard navigation contract change?", "Did the user data fetching logic change?"]
          },
          "confidencePrompt": {
            "enabled": false,
            "scaleMin": 1,
            "scaleMax": 5,
            "prompt": "How clearly did you identify which unit absorbs each type of change?"
          }
        }
      ],
      "workedExamples": [],
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-component-patterns"],
        "requiredPerformance": {
          "minimumAccuracy": 0.80,
          "minimumTransferScore": 0.75
        }
      }
    },
    {
      "experienceId": "s06-exp-03",
      "title": "Self-Guided: Extract Behavior into a Headless Hook with renderHook Tests",
      "experienceType": "self-guided-exercise",
      "difficultyProfile": {
        "overall": 4,
        "conceptualComplexity": 3,
        "ambiguity": 4
      },
      "estimatedDurationMinutes": 25,
      "linkedCompetencies": ["component-design-patterns", "testing-architecture"],
      "desiredCognitiveActions": ["analysis", "synthesis", "reflection"],
      "contentBlocks": [],
      "interactionSequence": [],
      "workedExamples": [],
      "selfGuidedExercise": {
        "exerciseId": "s06-sge-01",
        "realWorldContext": "A UI component in a real codebase that mixes behavior and presentation — a dropdown, a modal trigger, a multi-select, an accordion, a date picker, or similar. The component should have at least: some open/close state, at least one user interaction (click, keyboard), and ideally some ARIA attributes.",
        "objective": "Extract behavior into a headless hook with prop getters, verify it with renderHook tests, then confirm that a visual redesign of the component requires zero changes to the hook and its tests.",
        "instructions": [
          "Find a UI component in a codebase you work with that mixes behavior and presentation in a single component. Good candidates: a dropdown menu, a modal with a trigger button, a multi-select input, an accordion, a date range picker.",
          "Identify all the behavior in the component: open/close state, active selection tracking, keyboard event handlers, and ARIA attributes. These all move to the headless hook.",
          "Create the headless hook. Key rules: return prop getters (getTriggerProps(), getMenuProps(), getItemProps(index)) rather than raw setters. The hook should make zero assumptions about DOM element types. No JSX, no import of any UI library.",
          "Write at least two renderHook tests: one for the initial state (isOpen is false, no item selected), and one verifying a state transition (calling open() sets isOpen to true, or calling getItemProps(1).onClick() sets activeIndex to 1). Tests must make no assumption about whether useState or useReducer is used internally.",
          "Rewrite the component as a pure presentational layer that spreads the hook's prop getters onto its elements.",
          "Now change one visual detail of the component — a CSS class name, a wrapper element type, an animation — and verify: zero changes to the hook, zero changes to the tests.",
          "Note any ARIA attributes that were missing in the original component that you added in the headless hook."
        ],
        "estimatedRealWorldDurationMinutes": 90,
        "materialsOrPrerequisites": [
          "A React codebase with at least one UI component mixing behavior and presentation",
          "React Testing Library with renderHook installed, or familiarity with the API to write test skeletons"
        ],
        "reflectionPrompts": [
          "Which ARIA attributes did you add in the headless hook that were missing from the original component? What accessibility gap did the original have?",
          "When you changed the visual markup of the component, did the renderHook tests require any changes? If yes, what does that tell you about the hook's encapsulation?",
          "Is there any behavior you couldn't extract into the headless hook because it was too tightly coupled to a specific DOM structure? What would you need to change architecturally to decouple it?",
          "Could you use this headless hook with a completely different visual implementation — for example, the same dropdown behavior applied to a native select element on mobile? What would that implementation look like?"
        ],
        "verification": {
          "mode": "reflection-submission",
          "isGating": false,
          "selfAssessmentCriteria": [
            "I extracted all behavior into a headless hook with prop getters and zero JSX",
            "I wrote at least two renderHook tests covering initial state and one state transition",
            "I rewrote the component as a purely presentational layer",
            "I verified that a visual change to the component required zero changes to the hook and its tests",
            "I noted at least one ARIA attribute that was missing in the original implementation"
          ]
        },
        "linkedCompetencies": ["component-design-patterns", "testing-architecture"]
      },
      "retrievalPractice": [],
      "transferExercises": [],
      "masteryVerification": {
        "assessmentIds": ["assess-component-patterns"],
        "requiredPerformance": {
          "minimumAccuracy": 0.80,
          "minimumTransferScore": 0.80
        }
      }
    }
  ]
}
